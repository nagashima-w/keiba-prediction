# 版数運用規約

本書は root(`package.json`)・`packages/app`(`@keiba/app`)の版数(semver)をいつ・誰が・どの区分で
上げるかを定める(Issue #44-D-1)。

## 背景: なぜこの規約が要るか

exe のファイル名は `packages/app/electron-builder.yml` の
`artifactName: keiba-ev-tool-${version}-portable.${ext}` により `packages/app/package.json` の
`version` から作られる。ところが版数は 2026-08-04(コミット `b99d10e`)に 1.0.0 → 1.1.0 へ手で
上げられて以来、94 コミット据え置かれていた。その間に組み合わせオッズ取得(D-2b)・券種横断の
予算配分(D-2c 第1〜4段)・確率の質の計測基盤という明確な機能追加が入った。**「手で上げる」運用は
既に一度確立され、そして破れている。** 本書はこれを構造的に立て直す。

## 区分の基準

| 区分 | 基準 |
|---|---|
| **major** | 既存の保存データ・設定が使えなくなる変更(settings.json を読めない/DB を開けない/エクスポート JSON の後方非互換) |
| **minor** | 利用者から見てできることが増える、**または分析結果の数値が変わる**変更 |
| **patch** | それ以外(不具合修正・内部改善・CI/文書のみ) |

「数値が変わる」を minor に含めるのは本リポジトリ特有の判断である。スコアリング(バイアス補正・EV計算)の
変更は「機能が増える」わけではないため機能追加とは言えないが、利用者は「前回と結果が違う」と気づく。
これを patch(=「気にしなくてよい変更」)に埋もれさせると、過去の分析結果との比較が版数だけでは
判断できなくなる。そのため minor 側に寄せる。

## 上げる主体とタイミング

- **主体**: オーケストレーター(プロジェクトマネジメント役)。
- **タイミング**: boss 承認後、承認印 `[PUBLISH-APPROVED]` を付ける最終コミットで上げる
  (`CLAUDE.md` 手順6)。
- **頻度**: **新しい内容を配布する公開1回につき必ず1回上げる。** ここでいう「公開」とは、
  package.json の変更を含む新規コミットに承認印 `[PUBLISH-APPROVED]` を付けて push し、
  dev-latest を更新することを指す。**`workflow_dispatch`(手動実行)による同一コミットの再送は
  「公開」に含めない。** コミットそのものが同一である以上、版数を上げる対象(新規コミット)が
  そもそも存在しない——つまり版数は上げない/上げられない(この結論はコミットの引用に依存せず
  単独で成立する)。参考として、CI の偶発失敗から再実行・再送で回復すること自体は本リポジトリで
  正当な運用として実績がある(例: `0882e77`「CI再実行」、`0bee388`「再送」)。ただしこの2件は
  いずれも新規コミット(差分ゼロ/コメントのみ)による再送であり、`workflow_dispatch` による
  同一コミット再送そのものの実例ではない(`workflow_dispatch` は git 上に新規コミットを作らない
  ため、ローカルの git 履歴だけでは過去の実行例を直接検証できない)。ここでは「失敗からの
  再実行・再送は正当な運用である」という一般論の裏付けとして引用している。#45(D-2)の機械検査でも
  同じ理由で `workflow_dispatch` を版数据え置き検査から除外している(#45 で実装済み。
  `.github/workflows/build-windows.yml` の版数据え置き検査ステップの `if:` に
  `github.event_name == 'push'` を含める形で、`scripts/release-gate.ts` の
  `version-bump-check` サブコマンドを workflow_dispatch 実行時にはスキップする)。
  人に残る判断は「major/minor/patch のどれか」だけであり、「上げるか上げないか」は
  判断させない(判断させると 1.1.0 が 94 コミット据え置かれた事故が再発するため)。

### 版数を上げるときの同時更新チェックリスト

版数を上げる際は、以下をすべて同時に更新する。**一つでも更新を忘れると `pnpm test`
(`packages/app/test/version-policy.test.ts`)が失敗する。この失敗は「テストが壊れている」の
ではなく「更新漏れをテストが正しく検出している」と読むこと。**

1. root `package.json` の `version`
2. `packages/app/package.json` の `version`
3. `docs/current-spec.md` の冒頭(「実際に実装されている現状(vX.Y.Z)」の箇所)
4. `docs/current-spec.md` のバージョン一覧行(「バージョン: ルート/アプリ `X.Y.Z`」の箇所)
5. `keiba-ev-tool-spec.md` の冒頭(「実装は vX.Y.Z に到達しており」の箇所)
6. `packages/app/test/version-policy.test.ts` の `EXPECTED_APP_VERSION` 定数。**あわせて同ファイルの
   テスト名に埋まっている版数 literal も直す**(定数だけを見て置換すると取り残す)
7. 本書に「次の正式版が X.Y.Z である根拠セクション」(`## 次の正式版が X.Y.Z である根拠`という
   見出し)を追加する(区分〈major/minor/patch〉の判断根拠を残す)
8. `scripts/test/release-gate.test.ts` の実プロセス起動テスト(AC8)が持つ版数 literal。
   **対象は2つのテストだが、書き換える literal は4つある**(「前提: …の version が X.Y.Z である」の
   テスト名と `expect(appPkg.version).toBe("X.Y.Z")`、「バージョン一致(vX.Y.Z)で終了コード0」の
   テスト名と `"vX.Y.Z"` 引数)。**「2箇所」を「2つの文字列」と読んで機械置換すると取り残す**
   (1.3.1 への版上げ時に boss の実測で判明。取り残しても `pnpm test` が落ちて検出される)

6 と 8 は上げた先の版数を実ファイルに直接ピン留めしている。これ自体は「是正が実際に行われたことの
直接検証」であると同時に、次の上げ忘れに対する意図的な摩擦として機能する。摩擦として残すからには、
その存在をこの操作者向け文書に明記する(このチェックリストがその対)。

**8 は 1.2.2 への版上げで初めて露見した抜けである**(#45 で AC8 の実プロセステストを追加した際、
このチェックリストへの追記が漏れていた)。テストが落ちて発覚したので実害は無かったが、
「チェックリストに載っていない版数 literal」が今後も増えうることの実例として記録しておく。

7 は `packages/app/test/version-policy.test.ts` の `hasVersionRationaleSection` によって、
`EXPECTED_APP_VERSION` に対応する見出しの存在が機械的に検査される(code-reviewer 一次レビューの
提案を一般化して採用。literal な版数を直接ピン留めする専用テストを版ごとに積み上げるのではなく、
汎用の不変条件にすることで「根拠を書く」こと自体を機械的に強制する)。

## 表記ルール

- **プレリリース識別子は使わない。** `X.Y.Z` のみを使う(`1.2.0-dev.1` のような識別子・
  `1.2.0+build.5` のようなビルドメタデータは付けない)。
- **`packages/core`(`@keiba/core`)は運用対象外・据え置き。**
- **root と app の版数は常に一致させる。**
- **正式版タグは `v<package.json の version>`**(例: `v1.2.0`)。

## なぜコミットメッセージ規約による自動判定を採らないのか

「機能追加か否か」をコミットメッセージやパスから自動判定する案(Conventional Commits 等)は
**採らない**。本リポジトリのコミットメッセージは日本語の自由記述であり、`機能D-2c第4段`・
`core/app:`・`CI:`・`レビュー観点:` のような多様な書き出しが既に混在している。決まった接頭辞
(`feat:`/`fix:` 等)を要求する運用は、94 コミット分の既存の書き方と、3体のサブエージェント
(tdd-implementer / code-reviewer / boss)の指示書をすべて書き換えるコストを要し、しかも自由記述の
実態がある限り誤判定(feat のつもりが fix と書かれる/その逆)は避けられない。コストに見合わないため
自動判定は採用しない。代わりに、#43 で機械的に一意になった「公開の瞬間」に紐づけ、区分の判断だけを
人に残す方式を選んだ。

## なぜ core を対象外にするのか

`packages/core`(`@keiba/core`)は `package.json` に `"private": true` を持ち、npm には公開して
いない。app からの参照は `"@keiba/core": "workspace:*"`(`packages/app/package.json`)のみであり、
pnpm workspace 内でシンボリックリンク解決されるため、`workspace:*` という指定自体が固定バージョンでの
依存解決を必要としない。つまり core の `version` フィールドはどの値であっても app のビルド・実行に
一切影響しない。exe のファイル名にも現れない(現れるのは app の version のみ)。上げても下げても
意味を持たない値を運用対象に含めると「上げ忘れ」の検知対象が無意味に増えるだけなので、対象外・
据え置き(現状 `0.2.0`)とする。

## 次の正式版が 1.2.0 である根拠(本タスクでの是正)

1.1.0 以降に入った変更は以下の通り機能追加であり(minor 相当)、破壊的変更(major 相当)は無い:

- 組み合わせオッズ取得(D-2b)・券種横断の予算配分(D-2c 第1〜4段)・確率の質の計測基盤——
  いずれも利用者から見て「できることが増える」変更。
- 破壊的変更が無いことは実測済み: 設定は `packages/app/src/main/settings-store.ts` の
  `coerceSettings(raw: unknown)` が既定値マージで検証するため、旧い `settings.json`
  (新フィールドを持たない)もそのまま読める。DB は `packages/core/src/ev/analysis-store.ts` の
  `migrateResultPayoutColumn` 等(`ALTER TABLE ... ADD COLUMN` による後方互換マイグレーション、
  既存行は NULL として読める)により、旧バージョンで作成済みの DB もそのまま開ける。

よって 1.1.0 → **1.2.0**(minor)が妥当と判断した。

## 次の正式版が 1.2.1 である根拠(Issue #45 での是正)

Issue #45 の変更は `.github/workflows/build-windows.yml`・`scripts/release-gate.ts`・
テスト・文書のみであり、`packages/app/src`・`packages/core/src` には一切触れていない。
上記の区分の基準に照らすと「利用者から見てできることが増える」「分析結果の数値が変わる」の
どちらにも該当せず、区分の基準表に従い **patch** が妥当である。

据え置きではなく実際に上げる理由: `dev-latest` には既に `keiba-ev-tool-1.2.0-portable.exe` が
公開済みであり、本タスク自身が新設する版数据え置き検査(`version-bump-check`)は、今回ビルドする
exe と同名のアセットが既に存在する場合に block する。版数を据え置いたまま本タスクの変更を
承認印付きで push すると、この検査自身が発火してジョブが失敗する。したがって 1.2.0 → **1.2.1**
(patch)への是正は、本タスクの変更を公開するための前提条件であると同時に、新ゲートが
正当な公開(=版数を上げた公開)を妨げないことの allow 経路の実地証明にもなっている。

## 次の正式版が 1.2.2 である根拠(Issue #31 での是正)

Issue #31 の変更は `packages/core/src/ev/`(`allocation-primitives.ts` に述語 `isUsableOdds` を新設し、
`bet-allocation.ts` の候補フィルタと `combo-bet-allocation.ts` の2箇所が委譲)とそのテスト・JSDoc のみで、
`packages/app/src` には触れていない。

**patch である根拠は「利用者から見た挙動が変わらない」ことではなく、「実データで数値が動かない」ことである。**
本修正は不正なオッズ値を持つ馬を候補から外す挙動変更を含むが、`placeOddsMin` に非有限値が入る到達経路は
現状存在しない(生成箇所は `expected-value.ts` の2箇所のみで、いずれも厳格な検証を通る)。したがって
実運用のどのレースでも配分結果は1円も変わらない。区分表の「**分析結果の数値が変わる**」(minor)には
該当せず、「それ以外(不具合修正・内部改善)」に当たる。

よって 1.2.1 → **1.2.2**(patch)が妥当と判断した。

なお本修正は**潜在的欠陥の是正**であり、到達経路のある実害の修正ではない。将来 `placeOddsMin` に
非有限値を書き込む経路ができた場合、この修正が初めて実データに効く。

## 次の正式版が 1.3.0 である根拠(Issue #15 での変更)

Issue #15 は、混在配分の買い目一覧を**上位20件の常時表示+残りの折りたたみ**に分ける変更である
(`packages/app/src/renderer/mixed-allocation-view.ts` の `splitAllocationsForDisplay` と、
`BatchAnalysisView.tsx` の描画)。

**minor である根拠**: これは**意図的な UI 挙動変更**であり、区分表の patch(「それ以外(不具合修正・
内部改善・CI/文書のみ)」)に当たらない。ただし「利用者から見てできることが増える」という説明は
**正確ではない** — 既定で表示される買い目はむしろ**減り**、代わりに展開操作が**増える**。
増減の向きにかかわらず、利用者が見る画面の既定の姿が変わる変更なので minor に置く。

**「分析結果の数値が変わる」には該当しない**: 配分計算(`packages/core/src/ev/**`)には1行も
触れておらず、`totalStake`・券種別内訳・注記はいずれも分割の前後で同一である(不変式1〜3として
テストで固定した)。変わったのは**画面に何行出すか**だけである。

## 次の正式版が 1.3.1 である根拠(Issue #52 での変更)

Issue #52 は、レース結果ページからワイド・三連複の**確定払戻を取り込んで永続化**する変更である
(`packages/core/src/scraper/parse-race-result.ts` の `parseComboPayoutRow` と、
`packages/core/src/ev/analysis-store.ts` の `race_combo_payouts` / `race_combo_payout_imports`)。

**patch である根拠**: 区分表の minor は「**利用者から見てできることが増える**」または
「**分析結果の数値が変わる**」だが、本変更は**どちらにも該当しない**。UI・IPC は一切触っておらず
(過去分析の再表示は #55 がスコープ)、`ev/verify.ts` も無改変なので**回収率を含む分析結果の数値は
1つも変わらない**。取得・永続化という内部能力は増えるが、**利用者からは観測できない**。
よって区分表の patch(「それ以外(不具合修正・内部改善・CI/文書のみ)」)に該当する。

**major でない根拠**: 新テーブルは `CREATE TABLE IF NOT EXISTS` の追加のみで、旧DBはそのまま開けて
既存データも読める(旧DB互換をテストで固定済み)。「既存の保存データ・設定が使えなくなる変更」に
当たらない。

なお本 Issue は**保存はするが検証(`ev/verify.ts`)には一切反映しない**という中間状態で着地している。
反映は #54 が担う。`docs/current-spec.md` の「記録・回収率検証: 複勝のみ」は本 Issue 完了時点でも
**まだ正**であり、その微妙な状態を同文書に明記した。

## 次の正式版が 1.3.2 である根拠(Issue #61 での変更)

Issue #61 は、配布 exe で better-sqlite3 のネイティブバインディング(`.node`)の解決を
`bindings` パッケージの**スタックトレース推測**に任せるのをやめ、packaged 実行時は絶対パスを
明示指定する変更である(`packages/app/src/main/ipc.ts` の `resolveNativeBindingPath` /
`resolveVerifiedNativeBindingPath` と、`pipeline-deps.ts` の `new Database` 第2引数)。
あわせて `.node` が見つからないときの診断に、期待した絶対パス・`fs.existsSync` の結果・
`process.resourcesPath`・`app.isPackaged` の4要素を必ず含めるようにした。

**patch である根拠**: 区分表の minor は「**利用者から見てできることが増える**」または
「**分析結果の数値が変わる**」だが、本変更は**どちらにも該当しない**。スコアリング・EV 計算・
確率モデルに一切触れておらず、変更は解決関数2つと `new Database` の第2引数のみで、
**分析結果の数値は1つも変わらない**。よって patch(「不具合修正・内部改善」)に該当する。

**major でない根拠**: 既存の設定・DB・エクスポート JSON の後方互換は一切変わらない。
DB スキーマにも触れていない。(「DB を開けない」は major の例に挙がっているが、これは
*互換を壊す変更*を指すのであって、*開けない不具合を直す変更*ではない。)

**⚠️ この版は「修正」であると同時に「診断」である。** Issue #60 で報告された実機バグの
原因は**未確定**であり、本変更で直るとは限らない。着手前ゲートで実物の配布 exe を検査した
結果、`.node` は `app.asar.unpacked` に正しく展開されており `bindings` は正しく解決できて
いた(= 報告されたエラー文と成果物の中身が整合しない)。直らなかった場合、エラーの文面が
「経路13件を試した」形から「期待した絶対パス + `fs.existsSync: false`」に変わるため、
**原因を一発で確定または排除できる**ことが本版の主要な価値である。

## 次の正式版が 1.3.3 である根拠(Issue #62 での変更)

Issue #62 は、CI が生成した配布 exe に対して**成果物そのものを検査する2つのゲート**を追加する変更である
(`scripts/artifact-gate.ts` の asar 配置検査と、`scripts/artifact-gate-smoke-child.cjs` による
ヘッドレススモーク)。あわせて `packages/app/src/main/ipc.ts` にあったバインディング解決関数を
electron 非依存の `packages/app/src/main/native-binding.ts` へ抽出した。

**patch である根拠**: 変更の実体は `scripts/`・`.github/workflows/`・テスト・文書であり、
`packages/app/src` への変更は**関数の移設1件のみ**である。移設した関数本体は旧 `ipc.ts` と
**バイト単位で完全一致**しており(boss がメタレビューで実測)、`ipc.ts` は再エクスポートのみを行う。
スコアリング・EV 計算・確率モデル・UI には一切触れていないため、区分表の minor
(「利用者から見てできることが増える」または「分析結果の数値が変わる」)の**どちらにも該当しない**。
よって patch(「不具合修正・内部改善・CI/文書のみ」)に該当する。

**major でない根拠**: 設定・DB・エクスポート JSON の後方互換に関わる変更が一切ない。
DB スキーマにも触れていない。

**この版が「守るもの」を取り違えないこと。** 本検査は **#60 の症状を検出しない**。v1.3.1 の実物 exe
(#60 で壊れていたもの)に対して走らせても `allow` になることを実測で確認済みである
(A1/A2 とも PASS、`.node` は `unpacked: true`・1,918,976 バイトで実在し ABI も正常)。
#60 の真因である `bindings` パッケージのスタック走査は #61 で除去済みであり、本検査は成果物を
**現行(#61 後)のメカニズムで**叩くため、あの壊れ方は原理的に再現しない。本検査が守るのは、
**#61 が新たに単一障害点にした「ハードコードされた絶対パスが実物の配置と一致すること」**と、
ABI・asar・配置の各クラスである。#61(v1.3.2)が単一障害点を作り、#62(v1.3.3)がそれを
機械検査で守る、という対になっている。

## 次の正式版が 1.3.4 である根拠(Issue #57 での変更)

Issue #57 は、配分計算の純関数を `packages/app/src/renderer/` から `packages/app/src/shared/` へ
移設し、renderer と main が同一実体を呼べる状態にする変更である(#54 で回収率の記録を main 起点に
するための前提)。あわせて `shared/**` の相対 import が `shared` の外を指さないことを機械で守る
構造ガードを追加した。

**patch である根拠**: 区分表の minor は「**利用者から見てできることが増える**」または
「**分析結果の数値が変わる**」だが、本変更は**どちらにも該当しない**。移設は挙動不変であり、
UI・IPC・DB・スコアリング・EV 計算のいずれにも触れていない。区分表の patch
(「それ以外〈不具合修正・内部改善・CI/文書のみ〉」)に該当する。

**挙動不変の証拠が通常より強い**。boss がメタレビューで `comm -23` を用い「移設先のファイルに、
移設元に存在しない行が何行あるか」を数えた結果:

| ファイル | 旧ファイルに存在しない行 |
|---|---|
| `shared/race-allocation.ts` | **0行** |
| `shared/mixed-race-allocation.ts` | **0行** |
| `renderer/mixed-allocation-view.ts` | **0行**(宣言部は完全無改変) |
| `renderer/bet-allocation-view.ts` | **1行のみ**(boss が更新を命じた所在記述) |

**新規に書かれたロジック行はゼロ**である。あわせて renderer バンドルの生サイズが
275.43 kB でベースラインと完全一致し、**コンテンツハッシュ(`index-UjGiauZZ.js`)まで同一**
であることを確認した。テスト件数は 3493 → 3501(+8)で、増分は新設した構造ガードの
自己検証と positive control のみ、減少は0件である。

**major でない根拠**: 設定・DB・エクスポート JSON の後方互換に関わる変更が一切ない。
移設したのは純関数とその型だけであり、保存形式には触れていない。

**このタスクで確立した検証手段を記録する。** #57 の差し戻しは全6件が「散文が事実と食い違った」
ことによるもので、コードのロジックに対する指摘は1件も無かった。そこで人の判断を機械判定に
置き換える手段が2つ見つかっている:

1. **`comm -23` で「旧ファイルに存在しない行」を抽出する**。移設が本当に移動だけかを、
   宣言単位の diff より強く証明できる
2. **`git diff -M --find-renames=30% --summary` の `rename` / `create mode`**。
   「移動(ファイル名不変)」と「分離(新規ファイルへの抽出)」の言い分けを機械が決める。
   **ただし判定は閾値依存である** — 同じファイルが `937037f` 時点では 97%、JSDoc が伸びた
   `ca8155c` では 94% と下がっており、既定値(50%)を大きく下回るまで編集が加われば
   `rename` 判定は消える。閾値を明示して実行し、`git log --follow` を裏取りに併用すること

## 次の正式版が 1.3.5 である根拠(Issue #58 での変更)

Issue #58 は、配分の「見送り理由」を DB に保存しても意味が壊れない**安定コード**として
表現できるようにする変更である(永続化そのものは #59)。core の `BetAllocationResult` /
`GeneralBetAllocationResult` に `skipReasonCode` を追加し、日本語文言をそこから一方向に
導出する形にした。app 側には `AllocationOutcomeCodes`(層1の到達状態と層2の見送り理由を
混ぜない5フィールド)を追加した。

**patch である根拠**: 区分表の minor は「**利用者から見てできることが増える**」または
「**分析結果の数値が変わる**」だが、本変更は**どちらにも該当しない**。UI・IPC・DB・
スコアリング・EV 計算のいずれにも触れておらず、`git diff --stat -- packages/app/src/renderer/`
は空である。既存の日本語 `skipReason` も据え置きで、表示はそのまま変わらない。

**「公開 API へのフィールド追加だから minor ではないか」という論点について。** boss が
メタレビューで提起したが、patch と判断した。理由は2つある。(a) 区分表の基準は
「**利用者から見て**」であり、API 利用者から見てではない。本リポジトリは npm に公開して
おらず、`packages/core` は本書が明記するとおり**版数運用の対象外**である。(b) 追加した
`skipReasonCode` は既存フィールドを一切変えず、読まない側は影響を受けない。

**major でない根拠**: 設定・DB・エクスポート JSON の後方互換に関わる変更が一切ない。
DB スキーマにも触れていない(永続化は #59)。

**この版で守られるようになったこと。** 従来 `skipReason` は日本語文言だけで、DB に保存すると
文言を変えた瞬間に過去データが突き合わせ不能になる状態だった。本版で、文言はコードから
導出される一方向依存になり、**逆向きの依存が型と制御フローで書けなくなった**。
両側変異注入(文言5定数 + `buildCapTooSmallReason` の計12箇所を番兵文字列に置換)により、
(i) コード側テストが緑のまま、(ii) 既存の文言アサーション22件が赤くなることを実測している。
片側だけでは緑が空虚になるため、両側を見ている。

**レビューで見つかった、受け入れ条件そのものの穴を記録する。** boss が定義した AC1 の
タプルは `(route, fallbackReason, skipReasonCode, comboOdds)` の4要素だったが、
`AllocationOutcomeCodes` は5フィールドであり、`unavailableReason` が漏れていた。実装は
ブリーフどおりで正しかったが、**この1フィールドを null に潰しても全テストが緑のまま**
だった。`unavailableReason` が運ぶ `PlaceBetUnavailableReason` は
`not-sold` / `two-place-only` / `unknown` の3値で、**「判定不能と判定結果を混同しない」
(#31)そのものの区別**を運ぶ。#57 の「散文が宣言した契約がテストに届いていない」と同型で、
今回はずれた散文が**受け入れ条件そのもの**だった。

再発防止として `.claude/agents/boss.md` に「受け入れ条件が構造体・タプルの一部だけを
検査する形になっていないか」という観点を追記した。あわせてテスト側の射影を
`Object.keys(outcome)` 駆動に変え、フィールドが増えても自動的に追随するようにした
(型レベルの網羅性チェックでは「テストコード側が手で列挙し忘れること」は防げないため)。

## 次の正式版が 1.3.6 である根拠(Issue #59 での変更)

Issue #59 は、配分提案を分析保存と**同一トランザクション**で DB に永続化する変更である
(#58 が用意した安定コードの保存先を作る)。`analyses` に紐づく2テーブルを追加した。

- `analysis_allocation_meta`: 経路・見送り理由・設定値の20列。**全経路で必ず1行書く**
  (place-only / mixed / invalid のいずれでも「判定していない」状態を残さない)
- `analysis_bets`: `stake > 0` の明細のみ。`combo_key` は app 側で `buildComboOddsKey` を
  呼んで生成し、core は文字列として保存する(core は route / bet_type を解釈しない)

**patch である根拠**: 区分表の minor は「**利用者から見てできることが増える**」または
「**分析結果の数値が変わる**」だが、本変更はどちらにも該当しない。読み出し API・UI・
verify への反映はすべて明示的にスコープ外(#54 / #55 送り)で、画面上できることは1つも
増えない。配分計算のロジック・設定・既定値にも一切変更がなく、`analysis-pipeline.ts` の
`rows` 組み立ての位置移動も純粋な move であることを機械的に確認した(`comm -23` で本当に
消えた行は `// (5) 保存。` と `// (6) 結果組み立て(馬番昇順)。` の2行のみ)。

**major でない根拠**: 区分表の major は「既存の保存データ・設定が**使えなくなる**」であり、
「DB にテーブルが増えるか」ではない。本変更は `CREATE TABLE IF NOT EXISTS` による新規2本の
追加のみで、既存テーブルへの `ALTER` も列追加もゼロ。旧スキーマの DB を開いて既存分析が
読めることは AC4 のテストで固定した。逆方向(新版で保存した DB を旧版 exe で開く)も、
旧版は新テーブルを知らないだけで `analyses` / `analysis_horses` はそのまま読める。

**この版で守られるようになったこと。** 配分提案が分析と同じトランザクションで保存される
ため、「分析は残ったが配分だけ落ちた」という中途半端な状態が構造的に発生しない。
`deleteAnalysesWithUnknownPromptVersion` にも子→親の DELETE 順を追加した(FK が `NO ACTION`
かつ `foreign_keys=ON` のため、明示順がないと版不明の分析を削除できなくなる)。
配分計算の例外は `analysis-pipeline.ts` で捕捉して `route="invalid"` として保存するため、
**課金済みの LLM 分析を配分の失敗で失わない**。

### レビューが8巡した理由を記録する(判定基準の定式化が段階的に誤っていた)

本タスクは production コードが最初の実装コミット以降**1行も変わらない**まま、テストだけで
7回の差し戻しを受けた。原因は一貫して「**テストが全緑なのに写像の破壊を検出できない**」
同一の欠陥クラスで、判定基準の定式化が段階的に誤っていたことによる。最終形は次のとおり。

```
条件A0(前提): その束縛箇所の出力が、少なくとも1つのテストで「値として」比較されていること。
              述語検査(toBeGreaterThan / toMatch / toBeDefined)は A0 を満たさない。
条件A' (非定数): A0 を満たしたうえで、その束縛箇所が生んだ行の集合に2値以上あること。
                 「列」ごとではなく「束縛箇所(binding site)」ごとに判定する。
条件B  (非衝突): どの2列も、全行を通じて値ベクトルが完全一致しないこと。
```

判定は **JS 値として**行う。SQLite から読み出すと REAL の `1.0` と INTEGER の `1` は同じ
JS 数値になるため、**型が違うことは別物である根拠にならない**(実際に `ev_threshold` と
`include_wide` の束縛入れ替えが core 2071 件全緑で素通りした)。

**なぜ「列」ではなく「束縛箇所」なのか。** 1つの列の値ベクトルは、複数の分岐にある別々の
式が生んだ値の寄せ集めである。`betUnit` 列は3分岐が書いたリテラル `null` と3分岐が書いた
`DEFAULT_*.betUnit` の混合で、この `null` は「2値以上」を形式的に満たすが
`DEFAULT_*.betUnit` という束縛を一切拘束しない。

**なぜ A0 が要るのか。** A0 が破れていると、A' の検算は**入力側の値の多様性を数えて** OK を
返す。`placeBetsOf` の `stake` は入力が7値だったが、テストは `toBeGreaterThan(0)` という
述語検査しか持たず、`stake: 100` の直書きが app 1404 件全緑で素通りした。`stake` は #54 の
回収率計算の分母そのものであり、退行しても過去分は再計算できない(分析は課金済み LLM
呼び出しの結果)。

**この誤りは実装者・code-reviewer・boss の3者全員で独立に起きた。** 1人の注意力の問題では
なく、枠組みが欠けていたことの証拠として記録する。判定基準の機械化の可否は #69 で扱う。

**テストで強制できないと判定した箇所**(塞げなかったのではなく、塞げないと判定した):
`modelId` / `modelApproximate` は `PlaceJointModel` の実装が1つだけで単一値しか取り得ない。
`DEFAULT_BET_ALLOCATION_CONFIG.*` と `DEFAULT_GENERAL_BET_ALLOCATION_CONFIG.*` の参照先一致は、
両者が同一の定数を参照しており値として区別できない。詳細は `allocation-record.ts` の
JSDoc「## テストで強制できない箇所」に記録した。

## 次の正式版が 1.4.0 である根拠(Issue #70 での変更)

Issue #70(#54-A)は、`computeHorseBetOutcome` が返していた単一の `isPlaced` を
**`isInTopThree`(3着以内)**と**`isPlaceHit`(複勝の払戻対象。規則H)**に分離し、
#50 / #51 を是正する変更である。

**minor である根拠**: 区分表の minor は「利用者から見てできることが増える」または
「**分析結果の数値が変わる**」であり、本変更は**後者に該当する**。#51 の是正により、
NAR の5〜7頭立てレース(複勝は2着まで)で3着に入った馬に複勝払戻を付けて計上していた
過大計上が消えるため、**検証画面の複勝回収率が下がる方向に変わる**。この経路は
到達可能で、実データに影響する。

なお **#50 は数値を1つも動かさない**。`expected-value.ts:157` の
`isPositive = ev > threshold`(`ev = placeProb * oddsMin`)により、`oddsMin` が
`0` / 負 / `NaN` なら `ev > 1` が false でそもそも賭けず、`+Infinity` だけが
`isPositive=true` かつ `!isUsableOdds` になるが、`parse-odds.ts:57` の正規表現
`/^[0-9]+(\.[0-9]+)?$/` は `Infinity` を生成できない。**#50 は防御的堅牢化である。**

**major でない根拠**: 保存済みデータ・設定の互換に関わる変更が一切ない。DB スキーマにも
触れていない。`VerifyBetSummary` に `unjudgedOddsCount` を追加したが、既存フィールドは
すべて据え置きで、読まない側は影響を受けない。

### 分離が必要だった理由(1つの値が2つの概念を運んでいた)

修正前、`isPlaced` は **4か所**で使い回されていた。

| 読み手 | 何を測っているか |
|---|---|
| キャリブレーション表 | AI が推定した**3着以内確率**の当たり外れ |
| 補正傾向サマリ(`directionGroups`) | 同上 |
| 印別的中率(`markStats`) | 同上 |
| 払戻計上(`totalReturn` / `payoutSource`) | **複勝の払戻対象か** |

**6頭立てで3着に入った馬は、上3つでは的中、4つ目では不的中である。**#51 を
`computeHorseBetOutcome` の中で素朴に直すと、**キャリブレーション表がテスト全緑のまま
静かに壊れる**(3着馬が「予測が外れた」側に計上され、複勝率が下振れする)。
#31 の「判定不能と判定結果を混ぜない」と同型の欠陥である。

### 規則H(複勝的中判定)

レースに `place_payout` 非 null の行が**1件以上あれば**、的中はその馬の `place_payout` が
非 null かどうかで決める(**着順を見ない**)。無ければ従来どおり着順と近似払戻。

**出走頭数は DB から復元できない。** 中止・除外・取消の着順は `result-import.ts:34-37` で
すべて `null` に潰され、`race_results` に区別を残す列が無い。「行数」は取消/除外を含む頭数、
「非 null 着順の数」は完走頭数で、どちらも出走頭数と一致しない。`resolvePlaceBetTarget` は
`packages/app/src/shared/` にあり core からは層をまたがずに参照できない。

**しかし #51 が必要としているのは出走頭数ではなく「複勝の払戻対象馬の集合」**であり、
それは `race_results.place_payout` が非 null の馬番の集合として厳密に取得できる
(公式払戻表そのもの。同着で4頭に払い戻される場合も正確)。

**#51 の真因**: 現行 JSDoc は「実配当が**未取込**の場合のみ近似する」という**レース単位の
意味論**を宣言していたが、実装は `actualPayout !== null` という**馬単位**の判定だった。
規則H はこのずれも同時に解消する(#57 で名指しされた「散文が宣言した契約がテストに
届かない」型と同じ)。

### 塞がないと決めたこと

**「払戻未取込かつ5〜7頭立て」の残余過大計上は塞がない。** 出走頭数が復元不能なため。
`listUnimportedRaceIds` が `race_results` の行の有無で判定するため、一度着順だけ取り込んだ
レースは払戻が後から入らない。ただし netkeiba は着順と払戻を同時に公開するため到達は稀。
**頭数の近似値を「出走頭数」と称して使わない。**

### レビューで見つかった、検出力の穴を記録する

本タスクは production コードが `e36588c` 以降1行も変わらないまま、テストだけで3回の
差し戻しを受けた。すべて「テストが全緑なのに写像・契約の破壊を検出できない」同一の
欠陥クラスである。

| 差し戻し | 素通りしていた変異 |
|---|---|
| 1巡目 | `unjudgedOddsCount` を `1` にハードコード → 70件全緑(`0` に潰すと落ちるのに `1` では落ちない**非対称な穴**) |
| 3巡目 | `directionGroups` を `isPlaceHit` へ → 71件全緑 |
| 3巡目 | `RaceBreakdownHorse.isPlaced` を `isPlaceHit` へ → **core 2075 + app 1404 全緑** |
| 3巡目 | `buildRaceBreakdown` の `racePayoutAvailable` を `false` 定数へ → 71件全緑 |
| 3巡目 | `unjudgedOdds` に `!racePayoutAvailable &&` を足す → 71件全緑 |

**3巡目の原因は「読み手のうち1つ(`markStats`)が固定されていれば分離の破壊は捕まえられる」
というオーケストレーターの論証だった。** これが捕まえるのは4箇所を**一斉に**取り違える変異
だけで、リファクタで実際に起きる**1箇所ずつの退行**には一切効かない。#59 の条件A0 は
「列ごとではなく**束縛箇所(binding site)ごと**に判定する」と定式化されており、
`isInTopThree` という1つの列に対し束縛箇所は4つあった。**変異1回で検算できたものを
論証で済ませたのが誤り**である。

最後の1件は、#70 が是正している #51 の真因(JSDoc の宣言と実装のずれ)を、**その是正差分
自身が新しい JSDoc で再現していた**もの。「実配当取込済みであっても動作自体は決定的」と
断定していたが、規則Uのテストは2本とも払戻未取込のレースしか使っておらず、この断定を通る
経路が1本も無かった。**断定を落とすのではなく固定する**方を選び、テスト名に JSDoc の
該当文言を含めて両者を結びつけた。

再発防止として `.claude/agents/code-reviewer.md` に3観点を追加した(`fb85e24`)。
「配り先」構造では読み手ごとに変異を注入する / 共通関数に新しい引数を通したらもう一方の
呼び出し元も確かめる / 新しく書いた JSDoc の断定文を1文ずつ変異で検算する。

## 次の正式版が 1.5.0 である根拠(Issue #71 での変更)

Issue #71(#54-B)は、**配分ベースの回収率(`proposedBet` 系)**を検証画面に出す変更である。
#57 → #58 → #59 と積み上げた永続化基盤が、**初めて画面の数値になった**。

**minor である根拠**: 区分表の minor は「**利用者から見てできることが増える**」または
「分析結果の数値が変わる」。本変更は**前者に該当する**。ワイド・三連複を含めた
「分析時点の設定で実際に提案した配分どおりに賭けた場合の回収率」を新規に表示する。

既存の `bet` 系(一律100円・複勝のみ)・キャリブレーション・補正傾向の数値は**1つも変わらない**。
Q-B(2026-08-21 のユーザー判断)が「既存 `bet` の**仮定**を変えずそのまま残し、内訳を
新フィールドで追加する」と定めたとおりである。

**major でない根拠**: 保存済みデータ・設定の互換に関わる変更が一切ない。DB スキーマにも
触れていない(#59 が作ったテーブルを**読むだけ**)。

### 2つの回収率は名前で規則を区別する

賭け金の仮定が違うので、名前から規則が分からないと必ず取り違えられる。

| フィールド | 仮定 | 券種 |
|---|---|---|
| `bet` 系(既存・意味を変えない) | EV プラス馬に**一律100円** | 複勝のみ |
| `proposedBet` 系(新設) | **分析時点の設定で提案した配分どおり** | 複勝 + ワイド + 三連複 |

**`bet` と `proposedBet` を足した値はどこにも作らない**(型でもフィールドでも表示でも)。
賭け金の仮定が違う2つを足した金額は何の量でもないため。
一方 `proposedBet` 内部の券種横断合計(`overall`)は作ってよい — 3券種は**同一の仮定**を
共有し、ケリー配分はそもそもポートフォリオとして意味を持つ。

### 母集団を4分類し、件数を必ず開示する

| 分類 | 判定条件 | `proposedBet` の分母 |
|---|---|---|
| 配分あり | `route IN ('place-only','mixed')` かつ `skip_reason_code IS NULL` | 含める |
| 見送り | 同上だが `skip_reason_code IS NOT NULL` | 除外(件数のみ開示) |
| 未到達 | `route IN ('unset','yoso','unavailable','invalid')` | 除外(件数のみ開示) |
| 記録なし | メタ行が無い(#59 以前の旧分析) | 除外(件数のみ開示) |

**判定は `route` を先に見る。** `skip_reason_code` を先に見ると `route="unset"` が
全部「配分あり」に混入する — `unreachedOutcome`(`mixed-race-allocation.ts:293`)が
`skipReasonCode: null` を返すためで、**既定 `bankroll=0` の利用者では全レースが該当する**。
「母集団はあるのに回収率が出ない」という最も避けたい表示になる。この罠は
テストで固定してある(`route` チェックを外す変異で3件失敗)。

**母集団0のとき `recoveryRate` は `null`** であり `0` ではない。UI では `formatRate(null)` が
`"-"` を出す。**母集団0は「判定不能」であって「損した」ではない**(#31 の原則)。

### 判定不能を件数として開示する(規則U)

的中/不的中を確定できない買い目は、件数・賭け金・払戻の**いずれにも計上せず**
`unjudgedCount` に別に数える。「確定できる」の唯一の根拠は**確定払戻の取込状態**である。
`imported` かつ非空で `combo_key` が含まれない場合は**不的中(払戻0)**であり判定不能ではない
— この2つを分けることが規則Uの肝。

**この件数は画面に出す。** `race_combo_payouts` は #52 以降に結果取込したレースにしか
無いため、**既存 DB のワイド・三連複はほぼ全件が判定不能**になる。件数を出さないと
「ワイド 0点」が「1点も提案しなかった」のか「提案したが全部判定できなかった」のかを
区別できない。母集団の開示と同じ理屈が一段下のレイヤーで効く。

### 意図的に読まなかった列

`analysis_allocation_meta` の**残り18列**と `analysis_bets.odds` / `ev` は読んでいない。
`odds` / `ev` は #55(過去分析の再表示 UI)のために保存されているもので、
**分析時点のオッズを払戻に使うと「回収率」ではなく「提案時点の期待値の再計算」になり**、
Q-C(実際に提案した配分額で実績を測る)に反する。

読む列を絞ったのは検証コストの理由でもある。#59 の条件B(非衝突)は**列数の2乗**で
難しくなるため、誰も読まない列に条件A0/A'/B のコストを払うのは #59 で8巡した失敗の再現になる。

### 残余(意図的に塞がない。#72 で扱う)

**取消・除外馬(`finishPosition === null`)を含む買い目は、現実には返還されるが不的中として
計上される。** 確定払戻表からは「返還」と「不的中」を区別できないため
(中止・除外・取消の着順は `result-import.ts` で**すべて `null` に潰される**)。
回収率はこの分だけ**保守的に下振れする**(過大評価はしない)。

**既存 `bet` 系は同じ状況で集計対象外にしており、両系で扱いが異なる。** 返還は的中でも
不的中でもない第3の状態であり、判定結果に潰すのは #31 が禁じている形である。

**これは受け入れ条件を書いた側の欠陥だった。** #54 の着手前ゲートが定めた AC-B3 は
判定不能の3状態しか列挙しておらず、返還という第4の状態を落としていた。実装は指示どおりで
正しい。挙動は変えず(正しい扱いは設計判断で、組合せ券では `combo_key` デコーダに触れる)、
**現挙動を値として固定するテスト**を入れた。#72 の3案(返還計上 / 判定不能 / 現状維持)の
**どれに変えてもこのテストが落ちる**ことを実測済みで、次に触る人が気づかないうちに規則を
変えることはない。

## 次の正式版が 1.6.0 である根拠(Issue #55 での変更)

Issue #55 は、検証タブ「レース一覧」の各レースの折りたたみ内に**「配分提案(分析時点)」を
新規に表示する**変更である。#59 が作った永続化(`analysis_allocation_meta`/`analysis_bets`)を、
#71(回収率の集計)とは別の目的――**その分析を行った時点で実際に何を提案していたかをそのまま
再表示する**――のために、新しい読み出しAPI `AnalysisStore.getStoredAllocation` 経由で読む。

**minor である根拠**: 区分表の minor は「**利用者から見てできることが増える**」または
「分析結果の数値が変わる」。本変更は**前者に該当する**。過去に分析したレースを検証タブで
開くと、その分析時点での買い目・金額・オッズ/EV・見送り理由・実効設定(総資金・1レース上限・
ケリー係数・EV閾値・ワイド/三連複/組合せオッズ取得のON/OFF・オッズ状態)が新たに見えるように
なる。分析結果の数値(prior・補正後確率・EV等)自体は一切変えていない。

**major でない根拠**: 保存済みデータ・設定・エクスポート JSON の後方互換に関わる変更が一切ない。
`analysis_allocation_meta`/`analysis_bets` のテーブル定義(#59 で確定済み)には触れておらず、
新設した `getStoredAllocation` は**既存の列を読むだけ**(新規カラム追加なし)。`settings.json` の
スキーマも変えていない。

**patch でない根拠**: 内部改善やドキュメントのみの変更ではなく、検証タブという既存画面に
利用者が直接目にする新しい情報(配分提案ブロック)が実際に増えている。

### 読む列と読まない列(#71 との役割分担)

`getStoredAllocation` はメタ行のうち**13列**
(`route`/`unavailable_reason`/`fallback_reason`/`skip_reason_code`/実効設定7項目
〈`bankroll`/`per_race_cap`/`kelly_fraction`/`ev_threshold`/`include_combo_odds`/
`include_wide`/`include_trio`〉/`bet_unit`/`odds_status`)+ `analysis_bets` の5列
(`bet_type`/`combo_key`/`stake`/`odds`/`ev`)を読む。`combo_odds_wide`/`combo_odds_trio`/
`greedy_steps`/`candidate_cap`/`model_id`/`model_approximate` の6列は読まない
(#71 と同じ「誰も読まない列にコストを払わない」原則。前者2つは表示予定が無く、
後者4つは利用者に意味の無い内部パラメータ)。

`getAllocationForVerify`(#71。`route`/`skip_reason_code`/`bet_type`/`combo_key`/`stake`の
5列のみ、`odds`/`ev`は読まない)とは**読む列の範囲が異なる別のクエリ**であり、
互いに変更の影響を与えない。#71 が `odds`/`ev` を読まないのは「回収率」ではなく
「提案時点の期待値の再計算」になることを避けるためだが、#55 は**回収率を一切計算しない**
(的中・払戻・回収率は#16/#71の領分のまま出さない)ため、分析時点の`odds`/`ev`をそのまま
表示することが目的に反しない。

### 表示状態は7状態+判定不能

記録なし/unset/yoso/unavailable/invalid/見送り(skip)/配分ありの7状態に加え、値の矛盾
(未知の`route`文字列、または`route∈{place-only,mixed}` ∧ `skip_reason_code=null` ∧
`bets=[]`)を「判定不能」として別枠で扱う(#31: 判定済みを未判定に潰さない)。

状態の下位理由・パラメータだけが欠けている場合(`unavailable_reason=null`、
`skip_reason_code="cap-too-small"` ∧ `bet_unit=null`、`route="unset"` なのに
`bankroll`/`per_race_cap`のどちらも0以下でない)は、状態自体は保持し、欠けた部分だけを
代替文言で明示する(判定不能へは倒さない)。**最後の1件(`route="unset"`の想定外パターン)は
実装当初は最終分岐が「1レース上限のみ0」と断定する欠陥を持っていたが、レビューで修正した。**

## 次の正式版が 1.6.1 である根拠(Issue #34 での変更)

Issue #34 は、単勝・複勝(`parse-odds.ts`)/ワイド・3連複(`parse-combo-odds.ts`)/地方
(`parse-nar-odds.ts`)の3パーサが個別実装していた人気(`ninki`)の数値化を、共有ヘルパ
`packages/core/src/scraper/ninki.ts` に統合する変更である。JSDoc は「非数値・`"0"`(欠損表現)は
`null`」と宣言していたが、`parse-odds.ts`・`parse-nar-odds.ts` の実装は `Number("0")` の `0` を
そのまま返しており、宣言と実装が食い違っていた(値域外の値〈人気は1始まりで `0` は値域外〉が
下流の欠損表現に落ちない経路が構造として存在したが、コミット済み全フィクスチャに人気 `"0"` の
実データは無く、実データでの発生は未観測)。

**patch である根拠**: `70dfd14`(v1.6.0)から本Issueのレビュー往復が確定した時点(`b8c5d7b`)までの
差分は9ファイル(`git diff --stat 70dfd14 b8c5d7b` で実測)で、内訳は `packages/core` の
パーサ3本(`parse-odds.ts`/`parse-combo-odds.ts`/`parse-nar-odds.ts`)・新設した共有ヘルパ1本
(`ninki.ts`)・テスト4本(`ninki.test.ts`・`parse-odds.test.ts`・`parse-combo-odds.test.ts`・
`parse-nar-odds.test.ts`)・開発運用ドキュメント1本(`.claude/agents/code-reviewer.md`)である。
本版数上げでは加えて次の3種を変更しているが、いずれもコード・テストの振る舞いを変えない:
`.claude/agents/tdd-implementer.md`(再発防止の追記。`禁止事項`・`完了報告の形式`の2箇所)、
`parse-combo-odds.ts` のJSDoc文中の範囲括り(「オッズ…下記1〜3の差分」の対象がずれていた
記述の是正。コード行・テスト行への変更なし)、および版数運用のチェックリストが要求する版数リテラル自体
(`package.json` 2件・`docs/current-spec.md`・`keiba-ev-tool-spec.md`・`version-policy.test.ts`・
`release-gate.test.ts`・本書)。**フィクスチャ由来の期待値変更はゼロ**:
`parse-combo-odds.test.ts` はヘッダJSDocのみでテスト本文は無改変、`parse-nar-odds.test.ts` は
新規テストの追加のみで既存行は無改変、`parse-odds.test.ts` で変更した唯一の期待値
(`ninki: 0` → `ninki: null`)は `buildOddsJson` による**合成データ**への変更であり、実データ
フィクスチャへの出力ではない。したがって**観測済み実データに対する出力は1バイトも変わらない**。

**minor でない根拠**: 利用者から見てできることは増えない(UI・IPCに変更なし)。分析結果の数値
(prior・補正後確率・EV・配分提案等)も一切変わらない。

**major でない根拠**: 保存済みデータ・設定・エクスポートJSONの後方互換に関わる変更が一切ない。
DBスキーマにも触れていない。

よって 1.6.0 → **1.6.1**(patch)が妥当と判断した。

## 次の正式版が 1.6.2 である根拠(Issue #73 での変更)

Issue #73 は、単勝・複勝(`parse-odds.ts`)/ワイド・3連複(`parse-combo-odds.ts`・
`parse-nar-combo-odds.ts`)/地方単複(`parse-nar-odds.ts`)/全戦績の単勝オッズ列
(`parse-horse-results.ts`)がオッズ文字列を数値化する処理を、共有ヘルパ
`packages/core/src/scraper/odds-number.ts` の `toOddsNumber` に統合する変更である
(再現: `grep -rl 'from "./odds-number.js"' packages/core/src/scraper | wc -l` → `5`
モジュール、呼び出し箇所は計13〈内訳・再現コマンドは `parse-combo-odds.ts` 冒頭JSDoc参照〉)。
是正前は `parse-odds.ts`・`parse-nar-odds.ts`・`parse-horse-results.ts` が桁区切りカンマ
(例: `"1,234.5"`)を`null`に落としており、カンマが現れるのは1,000倍以上の高オッズだけのため、
取りこぼしが高オッズ側(妙味の源泉)に系統的に偏っていた。

**patch である根拠**: boss がメタレビューで実フィクスチャ**28通り**
(`fixtures/odds_*.json`・`fixtures/nar_odds_*.html` 計23通り+
`fixtures/horse_results_*.json` 5通り。ファイル数は本書執筆時点で
`ls fixtures/ | grep -E "^odds_|^nar_odds_" | wc -l` → `23`、
`ls fixtures/ | grep "^horse_results_" | wc -l` → `5` と実測)を、旧実装(是正前)と
新実装(本Issue後)を同一プロセスに読み込んで比較し、**出力差分0件**を確認した。
`numberOrNull`→`toOddsNumber`の契約変更(桁区切りカンマ・指数表記・符号・先頭/末尾ドット)を
含めて、**観測済み実データに対する出力は1バイトも変わらない**。よって区分表の
「**分析結果の数値が変わる**」(minor)には該当せず、「それ以外(不具合修正・内部改善)」
に当たる。

**minor でない根拠**: 利用者から見てできることは増えない(UI・IPCに変更なし)。分析結果の数値
(prior・補正後確率・EV・配分提案等)も上記の実測どおり一切変わらない。

**major でない根拠**: 保存済みデータ・設定・エクスポートJSONの後方互換に関わる変更が一切ない。
DBスキーマにも触れていない。

**このタスクで見つかった、散文の列挙が実物と合っていなかった欠陥を記録する。** メタレビューで
2回差し戻しがあり(計5件の指摘。うち3件は「散文中の数・列挙が実物と合っていない」という、
本Issueが是正している欠陥類型そのものの再発)、いずれも production コードの正しさではなく
JSDoc・テストコメントに書いた数の検算不足が原因だった。代表例: (1) `parse-horse-results.ts`
の`numberOrNull`共用フィールドの列挙から`raceNumber`が漏れ「他7フィールド」と書いていたが
実際は8フィールド、(2) 「N経路すべてに委譲」という表現が定義の数(4定義+インライン1)を
指しており実際の呼び出し箇所(12〜13箇所)とは単位が異なっていた、(3) 再現コマンドに
埋め込んだ`grep`が自己参照(コメント行に書いた`grep`パターン自体が対象ファイルの中にあるため
自分自身にマッチする)を起こし、単一ファイル`grep -n`と複数ファイル`grep -rn`で出力形式
(`NN:`か`path:NN:`か)が異なることに気づかず「構造的に除外できている」という記述が事実と
食い違っていた。再発防止として `.claude/agents/tdd-implementer.md` に「配線の網羅を主張する数は
呼び出し箇所の単位で数え、走査は概念で行う」観点を追記した。

よって 1.6.1 → **1.6.2**(patch)が妥当と判断した。

## 次の正式版が 1.6.4 である根拠(Issue #76 での変更)

Issue #76(#23-A)は、買い目の券種を `umabans.length`(1=複勝 / 2=ワイド / 3=三連複)から
逆算する構造をやめ、`AllocationCandidate` と `GeneralBetAllocation` の**両方**に
`betType` を必須フィールドとして持たせた。あわせて `verify.ts` が未知の券種コードの行を
無言で `continue` していた穴を、`ProposedBetReport.unknownBetType` として独立に計上する
形で塞いだ。

### patch と判断した理由

- **major に該当しない。** DB スキーマ・`settings.json`・エクスポート JSON はいずれも不変。
  `analysis_bets.bet_type` に書かれる値も変わらない(`betTypeOfUmabans` が返していた値と
  候補が運ぶ `betType` は現行データで一致する)
- **minor に該当しない。** **配分額・EV・回収率の数値は1つも変わらない。** 本体(コミット1)は
  挙動不変であり、`git diff 59f52ad <コミット1> -- '**/*.test.ts' | grep '^-' | grep -v '^---'
  | grep -v betType` の60行はすべて入力側の書き換えで、**期待値の右辺は不変**である
  (内訳: `buildComboCandidates` の引数型変更20 / `AllocationCandidate` リテラルへの
  フィールド追加34 / `mixedBetTypeLabel` の describe ブロック書き換え6)
- **AC-A5 が足す表示行(`unknownBetType` の注記)は、現行データでは常に非表示。**
  保存経路が `place`/`wide`/`trio` しか書かないため `count` が 0 のままであり、
  `formatUnknownBetTypeNotice` は `null` を返す
- `AllocationCandidate` への必須フィールド追加は `@keiba/core` の型としては破壊的変更だが、
  本書「表記ルール」のとおり **`packages/core`(`@keiba/core`)は版数運用の対象外・据え置き**
  であり、root/app の版数区分は**利用者から見た基準**でしか判定しない

よって 1.6.3 → **1.6.4**(patch)が妥当と判断した。

## 次の正式版が 1.6.3 である根拠(Issue #74 での変更)

Issue #74 は、オッズの値域(1.0以上・0を含む1.0未満は値域外)の判定基準
`ev/allocation-primitives.ts` の `isUsableOdds` を `Number.isFinite(value) && value > 0`
から `Number.isFinite(value) && value >= MIN_VALID_ODDS`(`MIN_VALID_ODDS = 1.0`)へ
引き上げ、全呼び出し元(着手前ゲートで実測: 呼び出し6・モジュール3〈combo-bet-allocation・
bet-allocation・verify〉。本Issueで `build-prompt.ts`・`probability-quality-metrics.ts` の
2箇所を追加委譲)に同時適用する変更である。是正前は `expected-value.ts` の `evaluateHorse` が
`placeOddsMin===null` しか見ておらず、複勝オッズ下限が `0`(構文上は正当な数値だが値域外)の
馬を通して `ev = placeProb × 0 = 0` という「正常な判定結果」に潰していた(#31の「判定不能と
判定結果を混ぜない」原則違反。実データでの発生は未観測)。

**patch である根拠**: **到達可能な入力(コミット済み全フィクスチャが示す、この scraper が
実際に生成しうるオッズ値)で数値が1つも動かない**ことを実測で示す。

中央競馬(`fixtures/odds_*.json`。`presale`〈発売前〉2ファイルを除く9ファイル。
`data.odds` の各セル配列の全要素を対象に、実際にオッズとして読まれる index 0 だけでなく
index 1〈複勝・ワイドは上限、単勝・三連複は埋め草〉も含めて走査):

| 券種(betType) | ファイル数 | 馬番/組数(ファイルごと) | index 0 の最小値(ファイルごと) |
|---|---|---|---|
| `1`(単勝) | 3 | 14〜16 | 4.6〜5.4 |
| `2`(複勝下限) | 2 | 16 | 1.6〜1.9 |
| `5`(ワイド下限) | 3 | 10〜120 | 1.1〜6.6 |
| `7`(三連複) | 3 | 10〜560 | 2.4〜32.7 |

全9ファイル・全券種・全セル(index 0/1 双方)を通じて **`(0, 1)` に入る値は0件**。

**再現スクリプトを `scripts/measure-odds-json-range.mjs` としてコミットしてある**
(`node scripts/measure-odds-json-range.mjs` で誰でも同じ数を再現できる。抽出規則
〈`data.odds[betType][id]` の配列要素を全て数値化する〉はスクリプト冒頭のJSDocに明記)。
`presale`2ファイルは `data` が空文字列のため対象外。

地方競馬(`fixtures/nar_odds_*.html` 12ファイル): `class` 属性が単語境界で "Odds" を含む
要素(`<td class="Odds">`・ネストされた `<span class="Odds ">` の双方。中央側と条件を
揃えるため中央/地方の呼び分けをしない)の直接のテキストノードを、レンジ表現
(下限-上限)の両端まで含めて数値化する。**再現スクリプトを
`scripts/measure-nar-odds-range.mjs` としてコミットしてある**
(`node scripts/measure-nar-odds-range.mjs` で誰でも同じ数を再現できる)。実行結果:
全12ファイル・**464個の数値**を走査し、最小値は `1.2`、**`(0, 1)` に入る値は0件**。

**この個数(464)は、以前このセクションで「Node.jsスクリプトで実測」とだけ書いて
スクリプト自体をコミットしていなかったために再現性を疑われた経緯がある。** 実際に
出た数と、それぞれ誰がどの方法で得たかは次のとおり:

| 数 | 誰が・どの方法で | 内実 |
|---|---|---|
| **464** | 着手前ゲートの boss、および本節が指す `scripts/measure-nar-odds-range.mjs`(今回コミット)の出力 | 正しい抽出規則(本スクリプトのJSDoc参照) |
| **440** | code-reviewer が本番パーサ(`parseNarOdds`/`parseNarComboOdds`)を使って独自に算出 | **presale(発売前)の組合せ券2ファイルの扱いが異なる**(下記) |
| **474・最小値1** | オーケストレーター(メイン)が、文書の散文の記述だけを見てその場で独自に書いた正規表現による値 | **単なるバグ**(下記) |

**440(本番パーサ)と464(本スクリプト)の差は「集約規則の違い」ではない。** 実際に
本番パーサ(`parseNarOdds`/`parseNarComboOdds`)を12フィクスチャそれぞれに通して
per-file の値を突き合わせたところ、**presale(発売前)の組合せ券2ファイルを除く10ファイルは
両者で完全一致**した(36 / 10 / 42 / 30 / 132 / 15 / 10 / 55 / 55 / 55。合計 440)。
差の 24 は `nar_odds_b5_presale_202655080803_20260806.html`・
`nar_odds_b7_presale_202655080803_20260806.html` の2ファイルだけに由来する
(`440 + 12 + 12 = 464` が算術で閉じることを確認済み)。この2ファイルは
`parseNarComboOdds` に通すと **`state: "unavailable"`**(`reason: { cartCountFound: false,
viewFormWrapperFound: true, oddsCellCount: 12 }`。実際に関数を呼んで確認した実測値)を
返し、組合せオッズとしては0件になる。中身は単勝の「予想オッズ」プレビューであり
(`parse-nar-combo-odds.ts` のJSDoc「本当の未発売」の節に、この具体的なファイル名で
`cartCountFound=false`・`oddsCellCount=12` になることが明記されている)、実物の
`<td class="Odds">` セルを確認したところ id 属性を一切持たない(ワイド・3連複のセルは
`id="chk_a1-...`のようにid属性で組を識別するため、id が無いセルは`parseNarComboOdds`の
組合せとして1件もマッチしない)。一方 `scripts/measure-nar-odds-range.mjs` はこの2ファイルの
`td.Odds` セル(各12件)を id 属性の有無を問わず機械的に数えるため、464 に含める。
**優劣ではなく「発売前プレビューのセルを組合せオッズとして数えるか」という対象の違い**であり、
「複数セルを集約する」という機序ではない(この誤った機序の記述は前回の版に残っていたもので、
本節で訂正した)。

**474・最小値1 は「別の抽出規則による食い違い」ではなく、メインが書いた正規表現の欠陥だった。**
メインの正規表現 `/[0-9]+(?:\.[0-9]+)?/g` は桁区切りカンマを処理しておらず、
`"1,061.8"` を `["1", "061.8"]` という**2個の数値**に分断する(再現:
`node -e 'const s="1,061.8"; console.log([...s.matchAll(/[0-9]+(?:\.[0-9]+)?/g)].map(m=>m[0]));'`
を実行すると `[ "1", "061.8" ]` になることを確認済み)。フィクスチャの `td.Odds` セルには
桁区切りカンマ入りの値が **`nar_odds_b7_202654071210.html`・
`nar_odds_b7_jiku1_202654071210.html` の2ファイルに各5箇所、計10箇所**ある
(再現: `grep -oE '<td class="Odds"[^>]*>[0-9,]+\.[0-9]+' <ファイル> | grep -c ","`)。
この10箇所が2個ずつに分断されるため `464 + 10 = 474` と一致し、分断で生じる
`"1,014.5"` 等の先頭断片("1"等)が本来1.0を大きく超える値であるにもかかわらず
最小値として拾われてしまう(`1.2` ではなく `1` になる)ことの両方に説明がつく。

**この経緯自体が記録価値を持つ。** #73 が是正したのはまさに「オッズのパースが
桁区切りカンマ非対応」という欠陥類型であり、その直後の本Issueで、検算用にその場で
書いたスクリプト(コミットしていない使い捨てコード)が**同じ欠陥類型を再生産した**。
検算のためのコードも本体と同じ欠陥を持ちうるという教訓として
`docs/lessons-learned.md` に残す(判断は同ファイル参照)。

**結論(`(0, 1)` に入る値が0件であること)は、464(本スクリプト)・440(本番パーサ)・
474(バグを含むメインの即興正規表現)の3通りすべてで一致しており、この結論自体は
最初から揺るがない。** patch 版数の根拠は無傷である。

**結論: コミット済み全フィクスチャ(中央9・地方12、計21ファイル)で、券種・中央/地方を問わず
1.0未満のオッズは1件も観測されない。** 既定のEV閾値(1.0)のもとでは、そもそも
`oddsMin∈[0,1)` の馬は `ev = placeProb×oddsMin < 1` で必ず `isPositive=false` だったため、
本Issueによる引き上げは「どの買い目を買うか」を1点も変えず、除外理由の文言と
`placeOddsMin`/`ev` の値の持ち方(判定不能 vs 判定結果)だけを変える。

**留意点(該当する既知の経路が無いことの明記。#50の先例に倣う)**: 利用者は
`settings.evThreshold` に1.0未満の値(例 0.5)を設定できる
(`packages/app/src/main/settings-store.ts` の受理条件は `n > 0`)。理論上は
「非既定の閾値 < 1.0」と「値域外〈(0,1)〉のオッズが実際に scraping される」の**両方**が
同時に成立して初めて、本Issue前後で `isPositive` の値が変わりうる。しかし後者は上記の
実測どおりコミット済み全フィクスチャで1件も観測されておらず、`scraper/odds-number.ts` の
構文解析(実際のHTML文字列をそのまま数値化するのみ)がこの値を作る経路も本Issueでは
変更していない。#50 が「`+Infinity` は正規表現から生成できないため到達不能」とした構造と
同型であり、**本Issueは防御的堅牢化である**(観測済み実データに対する挙動は変わらない)。

**留意点2(boss メタレビューR1・2026-09-04で発見。R2の指摘に応じて追記)**: 上記の留意点は
確定EV経路(`computeRaceEv`/`evaluateHorse`)についてのものであり、**推定EV経路
(`computeEstimatedRaceEv`/`evaluateEstimatedHorse`。発売前レース向け)には当てはまらない**。
推定EV経路は`isUsableOdds`のゲートが無く、`evThreshold`の設定値に**関係なく**
`isPositive=true`かつ`isUsableOdds`不成立になりうる(`placeConfig.coef`が非有限のとき。
実測は`expected-value.ts`の`computeEstimatedRaceEv`JSDoc「残余」参照)。ただし
`placeConfig`の供給元は`packages/app/src`に存在せず本番では常に既定(`coef=0.2`)が
使われるため、**この経路も運用上は到達しない**(状態分離は#23-Bへ)。本Issueが
patch(1.6.3)である根拠(到達可能な入力で数値が1つも動かない)は、確定EV経路(本節上記)・
推定EV経路(本段落)のいずれについても成立する。

**明示的な上書き(1.4.0根拠セクション末尾の記述との整合)**: 上記「1.4.0である根拠」セクションの
「`oddsMin` が `0` / 負 / `NaN` なら `ev > 1` が false でそもそも賭けず」という推論は、
**既定のEV閾値(1.0)を暗黙の前提にしており、一般には成り立たない**(閾値が1.0未満なら
`oddsMin∈(0,1)` でも `isPositive=true` になりうる)。本Issue(#74)で `isUsableOdds` の基準を
`>=1.0` へ引き上げたことにより、**`oddsMin∈[0,1)` は閾値の設定値に関わらず判定不能
(`ev=null`)になった**。この一文をもって当該記述を明示的に上書きする(過去の記録自体は
歴史的経緯として書き換えない)。

**minor でない根拠**: 利用者から見てできることは増えない(UI・IPCに変更なし)。上記の実測どおり、
分析結果の数値(prior・補正後確率・EV・配分提案等)は観測済み実データに対して一切変わらない。

**major でない根拠**: 保存済みデータ・設定・エクスポートJSONの後方互換に関わる変更が一切ない。
DBスキーマにも触れていない。`HorseEv.placeOddsMin`/`excludedReason` の**意味**(値域外でも
生の値を保持する)は変わるが、型定義(フィールド名・型)自体は変更していない。

よって 1.6.2 → **1.6.3**(patch)が妥当と判断した。

## 公開後の確認(版数据え置き検査が実際に走ったことの確認)

承認印付き push のあとは、CI run の「版数据え置きを検査(dev-latest 公開前)」ステップのログを必ず見る。
`版数据え置き検査: 問題ありません(公開を続行します)` が出ていれば、dev-latest の現行アセットとの比較が
実際に行われている。代わりに `::warning::` が出ている場合(設定不備・API 取得失敗)は、この検査は
fail-open でスキップされており版数据え置きを検出できていない(`scripts/release-gate.ts` の設計)。
exe が更新されたという事実だけでは「比較して問題なかった」のか「比較できずに素通りした」のかを
区別できないため、この確認は省略しない。

## 次の正式版が 1.6.5 である根拠(Issue #77・#20-A での変更)

Issue #77(#20-A)は、Plackett-Luce の潜在強度 θ の推定器(`fitPlackettLuceStrengths`)・
それを使う `PLACKETT_LUCE_MODEL`(`PlaceJointModel` の新実装)・`θ→1着確率` の純関数
(`winProbabilitiesFromStrengths`)を追加する変更である(`packages/core/src/ev/
plackett-luce-strength.ts`・`plackett-luce-model.ts`・`plackett-luce-win-prob.ts` の3ファイル
新設と、`place-joint-model.ts` からの re-export)。

**patch である根拠**: 区分表の minor は「**利用者から見てできることが増える**」または
「**分析結果の数値が変わる**」だが、本変更は**どちらにも該当しない**。

- **既定モデルは変えていない**: `bet-allocation.ts`・`combo-bet-allocation.ts` の既定引数は
  無改変で、両ファイルとも `model: PlaceJointModel = CONDITIONAL_BERNOULLI_MODEL` のまま
  (`grep -n "CONDITIONAL_BERNOULLI_MODEL" packages/core/src/ev/bet-allocation.ts
  packages/core/src/ev/combo-bet-allocation.ts` でデフォルト引数の行が変わっていないことを
  確認できる)。
- **`PLACKETT_LUCE_MODEL` の production 呼び出し元はゼロ件**(#20-A 時点。新設したモデルは
  テスト・ベンチスクリプトからのみ参照される)。よって既存のどのレースの配分結果・EV・
  回収率も1円たりとも変わらない。
- **`PlaceJointModel` インターフェース自体は無改変**(`buildDistribution`/`id`/`approximate`
  のシグネチャに変更なし)。`approximate` フィールドの JSDoc・`docs/current-spec.md` の記述は
  意味を明確化した(「UIが表記を出し分けるためのフラグ」→「同時分布が入力の周辺確率を再現
  しないかだけを表すフラグ。予測精度の保証ではない」)が、値そのもの・呼び出し契約は変えていない。
- **`packages/core/package.json` の `exports` は無改変**(新規ファイルは既存の唯一の公開サブパス
  `./ev/place-joint-model` から re-export する設計。`git diff --stat -- packages/core/package.json`
  が空であることで確認できる)。

**major でない根拠**: 保存済みデータ・設定・エクスポート JSON の後方互換に関わる変更が一切ない。
DB スキーマにも触れていない。

**AC-10(非破壊)の例外として扱ったファイル**: `packages/app/test/version-policy.test.ts` は
`packages/*/test/**` に一致するが、差分は**版数運用のチェックリスト(本書)が要求する
版数リテラル(`EXPECTED_APP_VERSION`)・it 名・it 名直後の版数履歴コメント3行(「#74 が
1.6.3、#76 が 1.6.4。本回は…」の記帳)のみ**であり(code-reviewerの指摘で「it名のみ」という
記述が実差分〈コメント3行も含む〉と食い違っていたことが判明し是正した)、既存の判定ロジック・
他のテストケースには一切触れていない。

よって 1.6.4 → **1.6.5**(patch)が妥当と判断した。

## 次の正式版が 1.6.6 である根拠(Issue #80・#78-A での変更)

Issue #80(#78-A)は、`PLACKETT_LUCE_MODEL` を含む任意の同時分布モデルが
`model.buildDistribution` で throw しても production のどの経路からも例外が外へ漏れない
ようにする変更である。受け皿は新設せず、既存の `route:"invalid"`/`kind:"invalid"` に一本化
した(`mixed-race-allocation.ts` に外側 try/catch を1つ新設、`mixed-allocation-view.ts` の
`resolvePlaceOnlyStake` に try/catch を1つ新設。既存の内側 catch はそのまま残す)。

**patch である根拠**: 区分表の minor は「**利用者から見てできることが増える**」または
「**分析結果の数値が変わる**」だが、本変更は**どちらにも該当しない**。

- **既定モデルは変えていない**: `bet-allocation.ts`・`combo-bet-allocation.ts` の既定引数は
  無改変で、両ファイルとも `model: PlaceJointModel = CONDITIONAL_BERNOULLI_MODEL` のまま。
  `packages/core/test/ev/default-model-invariant.test.ts`(本Issueで新設)が3つの公開入口
  (`allocateBets`・`allocateGeneralBets`・`buildComboCandidates`)それぞれで
  `modelId==="conditional-bernoulli"`(または既定モデルとの構造的同値)をリテラルで固定する。
- **throwしない限り、既存のどの入力・どのレースの配分結果・EV・回収率も1円たりとも変わらない**:
  受け皿は「モデルが契約違反でthrowした」という異常系にのみ作用し、正常系の計算経路
  (貪欲配分・ケリー基準・丸め処理等)には一切触れていない。
  `packages/app/test/mixed-race-allocation-crash-safety.test.ts`(本Issueで新設)の
  「黙っているべき側」describeが、throwしない限り既存の view/outcome が変わらないことを固定する。
- **`SkipReasonCode` は増やしていない**(6値のまま。`packages/core/test/ev/
  allocation-primitives.test.ts` に追加した `ALL_SKIP_REASON_CODES` の型検査により、
  型に新しい値を足すと`pnpm typecheck`がコンパイルエラーで検出する)。`verify.ts`・
  `VerifyView`・`formatUnjudgedNote`・DB スキーマは無改変。
- **`packages/core/package.json` の `exports` は無改変**(`git diff --stat -- packages/core/
  package.json` が空であることで確認できる)。

**major でない根拠**: 保存済みデータ・設定・エクスポート JSON の後方互換に関わる変更が一切ない。
DB スキーマにも触れていない。

**AC-10(非破壊)の例外として扱ったファイル**: `packages/app/test/version-policy.test.ts` は
`packages/*/test/**` に一致するが、差分は**版数運用のチェックリスト(本書)が要求する
版数リテラル(`EXPECTED_APP_VERSION`)・it 名・it 名直後の版数履歴コメントのみ**であり、
既存の判定ロジック・他のテストケースには一切触れていない(#77 と同じ扱い)。

よって 1.6.5 → **1.6.6**(patch)が妥当と判断した。

## 次の正式版が 1.7.0 である根拠(Issue #81・#78-B での変更)

Issue #81(#78-B)は、既定の同時分布モデルを `CONDITIONAL_BERNOULLI_MODEL` から
`PLACKETT_LUCE_MODEL` へ切り替える変更である。**production コードの変更は
`bet-allocation.ts`(:419 付近)・`combo-bet-allocation.ts`(:575・:903 付近)の
既定引数3行のみ**(`model: PlaceJointModel = CONDITIONAL_BERNOULLI_MODEL` →
`= PLACKETT_LUCE_MODEL`。`git diff --stat` で確認できる)。

**minor である根拠**: 区分表の minor(「利用者から見てできることが増える」または
**「分析結果の数値が変わる」**)のうち後者に該当する。既定モデルが変わることで、
model 引数を渡さない production の全呼び出し元(`allocateBets`・`allocateGeneralBets`・
`buildComboCandidates`)が返す配分結果(stake配列・betCount・isSkip・kellyTargetStake等)が
**新規分析から実際に変わる**(`packages/core/test/ev/model-characterization.test.ts` の
F1〜F6がCB/PL両モデルの数値差をリテラルで固定しており、F4は`isSkip`がCB=false/PL=trueと
反転する)。`modelId`/`modelApproximate` の保存値も `"conditional-bernoulli"`/`true` から
`"plackett-luce"`/`false` へ変わる(`allocation-record.test.ts`)。

**major でない根拠**: 保存済みデータ・設定・エクスポート JSON の後方互換に関わる変更はない。
`model_id`/`model_approximate` の DB 列は #80 時点で既に存在しており(スキーマ変更なし)、
過去に保存された分析行(`model_id="conditional-bernoulli"`)はそのまま読める。切替は
**新規分析以降にのみ**適用され、既存の保存データを書き換えたり読めなくしたりしない。

**この切替は「配分の精度が上がる」ことを意味しない**(#81 の位置づけそのもの)。根拠は
「同一の θ から1着確率・上位k集合確率・順列確率を導出できる」という #23-B・#25 の技術的前提
(1レース内で2つのモデルが混在するのを避けるための依存関係上の要請)であり、精度改善では
ない。むしろ `scripts/bench-joint-model.ts` の実測(`pnpm tsx scripts/bench-joint-model.ts`
で再現可能)では `marginalDeviationMax` の中央値が PL 0.061835 / CB 0.046275(clipVariant=
default・N=200)で PL が**悪化**し、PL が CB より悪化する割合は同条件で 117/199(58.8%)・
wide15 条件(N=200)で 143/200(71.5%)だった(自分で実行して確認した値。詳細は
`docs/current-spec.md`「5. 馬券配分の提案」参照)。

**設定項目は追加していない**(`MAX_FIT_ITERATIONS`・`FIT_TOLERANCE` も #80 の値のまま据え置き)。
**`SkipReasonCode` は増やしていない**(6値のまま。フィット非収束は既存の
`route:"invalid"`→`unreached`(判定不能)に分類され、`verify.ts`・`VerifyView`・
`formatUnjudgedNote`・DB スキーマは無改変)。#80 で新設した例外の受け皿(外側 try/catch・
`resolvePlaceOnlyStake` の catch・既存の内側 catch の計2箇所)も統合していない。
**`packages/core/package.json` の `exports` は無改変**(`git diff --stat -- packages/core/
package.json` が空であることで確認できる)。

**AC-10(非破壊)の例外として扱ったファイル**: `packages/app/test/version-policy.test.ts` は
`packages/*/test/**` に一致するが、差分は**版数運用のチェックリスト(本書)が要求する
版数リテラル(`EXPECTED_APP_VERSION`)・it 名・it 名直後の版数履歴コメントのみ**であり、
既存の判定ロジック・他のテストケースには一切触れていない(#77・#80 と同じ扱い)。

よって 1.6.6 → **1.7.0**(minor)が妥当と判断した。

## 次の正式版が 1.7.1 である根拠(Issue #88・#23-B0 での変更)

Issue #88(#23-B0)は、`estimatePlaceOddsMinFromWin` の戻り値を4状態の判別共用体
(`算出成功`/`単勝オッズ未確定`/`単勝オッズ値域外`/`算出値不正`)に変え、`evaluateEstimatedHorse`を
全kindのswitchにした変更である。

**patch である根拠**: 本番では `estimatedPlaceConfig` が `pipeline-deps.ts` から供給されず coef は
常に既定 0.2 であり、この固定値では `winOdds=Number.MAX_VALUE` でも算出値は約 3.60e307 で有限。
非有限 winOdds は算出前の「単勝オッズ値域外」分岐で弾かれるため、新設した `算出値不正` 分岐は
**本番で到達しない**。したがって**分析結果の数値は一切変わらない**。振る舞いの差分は
`HorseEv.excludedReason` のリテラル1件のみ
(`"推定複勝下限が不正な値(1.0未満・非有限)のため対象外"` →
`"推定複勝下限の算出結果が不正な値(非有限)のため対象外"`。「1.0未満」は `Math.max(1.0, …)` を
通るため構造上到達不能=常に偽の選言要素だった)。区分表の patch(「利用者から見て動作は変わらない
内部改善・バグ修正」)に該当し、minor の基準(できることが増える・分析結果の数値が変わる)には
該当しない。

**major でない根拠**: 保存済みデータ・設定・エクスポート JSON の後方互換に関わる変更はない。

よって 1.7.0 → **1.7.1**(patch)が妥当と判断した。

## 次の正式版が 1.8.0 である根拠(Issue #90・#23-B2 での変更)

Issue #90(#23-B2)は、単勝(win)を実際にアプリの配分提案へ組み込んだ変更である。
`packages/core/src/ev/combo-bet-allocation.ts` に単勝専用の候補ビルダー `buildWinCandidates`
(順序付き outcome 空間から1着確率を導出し、単勝オッズと掛けてEVを算出する。複勝〈3着内〉
確率では値付けしない)を新設し、`packages/app/src/shared/mixed-candidates.ts` の
`buildMixedCandidates`(`ALL_MIXED_CANDIDATE_BET_TYPES` に `"win"` を追加)・
`packages/app/src/shared/mixed-race-allocation.ts` の `resolveMixedBetTypes`(複勝と同じく
`"win"` を常時積む。専用のON/OFF設定は作っていない)から実際に配線した。

**#91(1.7.2)・#92(1.7.3)が「production からは到達不能」としていた型・内部経路
(`AllocationBetType` の `"win"`、`allocateGeneralBets` の順序付き outcome 空間経路)が、
#90 でその到達不能性を解消した**(直前の #91・#92 のセクション参照)。

**minor である根拠**:
- **できることが増える**: 単勝が実際にアプリの配分提案(`BatchAnalysisView.tsx`の券種別内訳・
  買い目一覧)に登場するようになった。以前は単勝オッズを複勝下限の概算にしか使わなかったが、
  単勝そのものが賭け対象になった。
- **分析結果の数値が変わる**: win候補が1件でもEVプラスとして存在すると、`allocateGeneralBets`は
  集合空間+畳み込み(`foldToCandidateSubsets`)ではなく順序付き outcome 空間を使う経路
  (`determined`分岐。#92で新設)に切り替わる。win・複勝・ワイド・三連複は同じ1つの予算枠
  (ケリー基準の貪欲逐次配分)を奪い合うため、**win自体の配分額だけでなく、複勝・ワイド・
  三連複の配分額も従来(winを含めない場合)とは変わりうる**。実際に変わることを
  `scripts/bench-mixed-allocation.ts`(保存済み実オッズフィクスチャ・ネットワークに出ない)で
  自分で測って確認済み(win追加前後でワイド・三連複の配分額・点数・合計額がいずれも変化する。
  具体的な数値は実行環境・実データ更新により変動しうるため本書には書かない。再現手順は
  同スクリプトの JSDoc「計測条件」参照)。

**major でない根拠**: 保存済みデータ・設定・エクスポート JSON の後方互換に関わる変更はない。
`analysis_bets`テーブルはスキーマ変更なしで`bet_type="win"`行が増えるだけであり
(`allocation-record.ts`の`mixedBetsOf`は`betType`列に候補が運ぶ値をそのまま書く既存実装のまま
無変更)、既存の複勝・ワイド・三連複の行の形・意味は変わらない。設定
(`MixedAllocationSettings`)にも単勝専用の項目を追加していない(7項目のまま。D-10裁定)。

**既知の制限(参考記録)**: 単勝は現時点で性能上のコストが大きい(win候補があると
順序付き outcome 空間〈P(出走頭数,3)〉を使うため、集合空間+畳み込み〈C(出走頭数,3)〉より
計算量が増える。`scripts/bench-mixed-allocation.ts`で再現できる実測で約7〜8倍の所要時間増加を
確認済み)。この対応は#90の受容判断(2026-09-22ユーザー決定: 「このタイミングでは受容するが
早いうちに対応する」)により、#90完了後に着手する別Issue(性能チューニング)のスコープとする。

よって 1.7.3 → **1.8.0**(minor)が妥当と判断した。

## 次の正式版が 1.7.3 である根拠(Issue #92・#23-B1b での変更)

Issue #92(#23-B1b)は、単勝を配分エンジンで扱うための**順序付き outcome 空間**を
`packages/core` に新設した変更である。主な追加は `OrderedOutcome` /
`OrderedPlaceJointModel` / `isOrderedPlaceJointModel`(`place-joint-model.ts`)、
`PLACKETT_LUCE_MODEL.buildOrderedDistribution`(`plackett-luce-model.ts`)、
`GeneralBetAllocationResult.winOutcome`(3値の判別共用体)と、
`allocateGeneralBets` の `determined` 分岐(順序空間から直接 isHit を判定し、
`foldToCandidateSubsets` を通さない経路)。あわせて `validateCandidates` の
重複判定キーに `betType` を含め、#91 の暫定門番(win を throw)を撤去した。

**patch である根拠**: 新設した経路は **production から到達不能**である。
`allocateGeneralBets` の production 呼び出し元は
`packages/app/src/shared/mixed-race-allocation.ts:444` の**1箇所のみ**で、そこへ渡る候補は
`buildMixedCandidates` 由来に限られる。`resolveMixedBetTypes`(同ファイル `:180-185`)が
組み立てる `betTypes` は `["place"]` に `wide`・`trio` を条件付きで足すだけで、
**`"win"` を積む分岐が存在しない**。したがって `winOutcome` は production では常に
`{ kind: "not-requested" }` であり、`determined`・`indeterminate` のどちらの経路にも
到達しない。**win 行を産む app 側の候補ビルダーは #90(#23-B2)まで存在しない。**

`winOutcome` は必須フィールドとして全結果に載るが、`allocation-record.ts` は
`GeneralBetAllocationResult` を丸ごとスプレッドせず `modelId` / `modelApproximate` /
`allocations` を**名指しで**読むため、**永続化されない**(保存済みデータの形は変わらない)。

以上から、**利用者から見える変化も、分析結果の数値変化も無い**。振る舞いの差分は
「型と内部経路が単勝を扱えるようになった」ことのみであり、区分表の patch
(「利用者から見て動作は変わらない内部改善・バグ修正」)に該当する。minor の基準
(できることが増える・分析結果の数値が変わる)には、**#90 で app 側が win 候補を
産み始めるまで**該当しない。

**major でない根拠**: 保存済みデータ・設定・エクスポート JSON の後方互換に関わる変更はない
(上記のとおり `winOutcome` は永続化されない)。

よって 1.7.2 → **1.7.3**(patch)が妥当と判断した。

## 次の正式版が 1.7.2 である根拠(Issue #91・#23-B1a での変更)

Issue #91(#23-B1a)は、`AllocationBetType`(`packages/core/src/ev/combo-bet-allocation.ts`)に
`"win"`(単勝)を追加し、`buildComboCandidates`・`validateCandidates` の両方に
`betType==="win"` 専用の門番(throw)を新設した変更である。

**patch である根拠**: `AllocationCandidate` を本番で生成している箇所は
`combo-bet-allocation.ts`・`packages/app/src/shared/mixed-candidates.ts` の**2箇所のみ**であり、
どちらも `"win"` を含む候補を産出できない。加えて `resolveMixedBetTypes`
(`packages/app/src/shared/mixed-race-allocation.ts`)が組み立てる `betTypes` は `["place"]` に
`wide`/`trio` を足すのみで `"win"` を積まない。したがって新設した2つの `throw` も、
`mixedBetTypeLabel("win")`(`packages/app/src/renderer/mixed-allocation-view.ts`)も**本番からは
到達不能**であり、**利用者から見える変化・分析結果の数値変化は一切ない**。振る舞いの差分は
`AllocationBetType` という型の許容範囲が広がったこと(型システム上の変化)のみであり、
区分表の patch(「利用者から見て動作は変わらない内部改善・バグ修正」)に該当し、minor の基準
(できることが増える・分析結果の数値が変わる)には該当しない。

**major でない根拠**: 保存済みデータ・設定・エクスポート JSON の後方互換に関わる変更はない。

よって 1.7.1 → **1.7.2**(patch)が妥当と判断した。

## 次の正式版が 1.8.1 である根拠(Issue #96 での変更)

**patch**(内部改善)。1.8.0 以降に入った変更は #96 の高速化のみであり、利用者から見てできることは
増えず、**表示される数値も変わらない**。

### 変更内容

win 候補があるときに使う順序付き outcome 空間(`P(n,3)`)に対して次の2つを入れた
(`packages/core/src/ev/allocation-primitives.ts`):

- **(A) 署名畳み込み** `foldOutcomeIndexSetsBySignature`: 的中パターン(`indices`)が同一の outcome の
  `probability` を合算する。`wealth_T` は `indices`・`x`・`odds` だけで決まるため、`indices` が一致する
  outcome は同一の `wealth` を与える。したがって `P1*log(w) + P2*log(w) = (P1+P2)*log(w)` が
  **厳密に成立する**(近似ではない)。
- **(B) メモ化** `runGreedyAllocation` 内で `Math.log(commonWealth[j])` と候補ごとの fresh wealth を
  それぞれ1回だけ計算して使い回す。式・項順・分岐は削除前の `computeFreshWealth` と逐語一致させており、
  「同じ引数に対する同じ関数値の再利用」なのでビット同一。

中央16頭で outcome **3360 → 1023**、Σ|indices| **12486 → 5386**。1レースあたりの所要時間は
約 1460ms → 約 370〜385ms(**約 3.8〜3.9 倍**)。

### minor ではない根拠(分析結果の数値が変わらないこと)

区分表の minor は「利用者から見てできることが増える、**または分析結果の数値が変わる**変更」だが、
本件はどちらにも当たらない。実測(boss のメタレビュー):

| フィクスチャ | 候補数 | outcome 畳み込み | `continuousFraction` 不一致 | `hitProb` 最大差 | 表示桁の変化 |
|---|---|---|---|---|---|
| `central-on` | 405 | 3360 → 1023 | **0/405** | 4.441e-16 | **0** |
| `central-off` | 14 | 3360 → 162 | **0/14** | 3.331e-16 | **0** |
| `nar-on` | 128 | 1320 → 331 | **0/128** | 1.943e-16 | **0** |
| `nar-off` | 8 | 1320 → 33 | **0/8** | 1.388e-16 | **0** |

配分額(`continuousFraction`)は全件ビット一致。`hitProb` のみ加算順序の違いで最終ビットが動くが、
表示桁は 1 件も変わらず、そもそも `hitProb` は `packages/app/src` から **0 参照**(UI に届かない
内部診断値)である。

**順序情報は破壊していない。** 畳み込みは `indices` 配列を一切書き換えず `probability` だけをまとめる。
#92 の裁定で win 同居時に使えないと判断した `foldToCandidateSubsets`(`placed` を昇順ソートして
順序を潰す)とは別物である。

### major ではない根拠

保存済みデータ・設定・エクスポート JSON・IPC・DB スキーマはいずれも無変更。`packages/core` の
純粋な計算経路のみの変更であり、`pnpm --filter @keiba/app build`(renderer / main / preload)も成功。


## 次の正式版が 1.9.0 である根拠(Issue #100・#23-C での変更)

**minor**(利用者から見てできることが増え、かつ**分析結果の数値も変わる**)。

### 変更内容

単勝(`bet_type="win"`)の確定払戻を DB に永続化し、回収率検証(`ev/verify.ts`)が単勝の買い目を
判定できるようにした。

- `race_results` に `win_payout REAL` 列を後付け(`migrateResultWinPayoutColumn`。既存の
  `migrateResultPayoutColumn` の逐語コピー)。`RaceResultEntry.winPayout` を追加し
  `saveResult` / `getResult` に配線
- `toResultEntries`(`result-import.ts`)に `winPayoutByUmaban` を追加(`placePayoutByUmaban` と同型)
- `verify.ts` に `raceHasWinPayout`(`raceHasPlacePayout` の鏡写し・**別関数として新設**)と
  `win` アキュムレータ、`winPayoutByComboKey` を追加。照合キーは `analysis_bets.combo_key` を作る
  `buildComboOddsKey` と**同一の関数**で生成する(ゼロ埋めロジックを書き起こさない)
- `ProposedBetReportView` に `win` を追加し、`VerifyView.tsx` の「内訳」行と
  「判定不能(集計対象外)」行の**両方**に単勝を表示

### minor である根拠

区分表の minor は「利用者から見てできることが増える、**または分析結果の数値が変わる**変更」だが、
本件は**両方**に当たる:

- **できることが増える**: 検証画面に単勝の内訳・判定不能が出るようになった。#90(v1.8.0)で単勝が
  配分提案に載るようになったが、払戻が DB に無いため判定できず、#76 の `unknownBetType` として
  「未対応の券種コード(win)」警告が出ていた
- **数値が変わる**: `proposedBet.overall` が win を含む**4券種**の合算になったため、**回収率の数値が
  変わる**(従来は単勝の投資額・払戻が `overall` に入らず集計から除外されていた)

### major ではない根拠

`race_results` への列追加は `ALTER TABLE ADD COLUMN` による後付けで、**旧 DB は開いたまま既存行を
保持する**。boss がメタレビューで**旧スキーマの実ファイル DB を作って実測**した:

```
既存行:   [{"umaban":4,"finishPosition":1,"placePayout":210,"winPayout":null}]
再取込後: [{"umaban":4,"finishPosition":1,"placePayout":210,"winPayout":670}]
再オープン: [{"umaban":4,"finishPosition":1,"placePayout":210,"winPayout":670}]
```

マイグレーションは正しく動き、冪等である。設定・エクスポート JSON・IPC の後方非互換も無い。

⚠️ **ただし、この旧 DB マイグレーションのテストはコミットされていない**(boss の実測はスクラッチ)。
同ファイル `analysis-store.test.ts` には旧スキーマ DB を開くテストの先例が7件あるが、
`win_payout` だけがそれを持たない。**【記録】として Issue に積んである。**
`CREATE TABLE IF NOT EXISTS` に列を足すと**新規 DB では必ず緑**になるため、
テストがすべてインメモリ新規である限りこの経路は自動では守られない。

### 既存 DB への反映方針

**(b) 以後の取込分のみ。** 過去レースの一括再取込は**利得ゼロ**である:
`bet_type="win"` の行を書けるようになったのは `3abe0f1`(#90, v1.8.0)が唯一なので、
それ以前に保存された分析には照合相手の買い目行が存在しえない。


## 次の正式版が 1.9.1 である根拠(Issue #103・#24-A での変更)

**patch**(調査タスク。`packages/*/src` を1行も変更していない)。

### 変更内容

馬連・馬単のオッズ取得経路を実測し、一次データを `fixtures/` に保存して
`docs/quinella-exacta-odds-investigation.md` に記録、`packages/core/test/scraper/` に
集合一致テストを追加した。#24(馬連・馬単)は着手前ゲートで **【No-Go】** となり6分割され、
本回はその第1子(#24-A)。

差分の内訳: `docs/` 1ファイル・`fixtures/` 10ファイル・`scripts/` 1ファイル(単発取得スクリプト)・
`packages/core/test/scraper/` 1ファイル(新規)。

### patch である根拠

- **利用者から見てできることは1つも増えていない**(賭け対象として馬連・馬単が使えるように
  なるのは #24-D / #24-E)
- **分析結果の数値も一切変わらない**
- **production のコードパスに到達する変更が無い**:
  `git diff --stat -- packages/core/src packages/app/src` が**空**であることを確認済み。
  既存 `packages/core/test/scraper/wide-trio-odds-fixtures.test.ts` も**無改変**(25件緑のまま)
- `scripts/fetch-quinella-exacta-raw.ts` は `pnpm test` / CI のどこからも参照されない単発スクリプト
  (`tsconfig.scripts.json` の `include: ["scripts"]` により `pnpm typecheck` の対象にはなる)

### major / minor ではない根拠

保存済みデータ・設定・エクスポート JSON・IPC・DB スキーマのいずれにも触れていない。

### このタスクで確定した事実(#24-B 以降の前提)

| 論点 | 確定内容 |
|---|---|
| 中央の取得経路 | `api_get_jra_odds.html?type=4`(馬連)/ `type=6`(馬単) |
| 地方の取得経路 | `odds/index.html?type=b4 / b6` |
| **1レースのリクエスト数** | **1**(馬連・馬単とも、中央・地方とも)。**3連複の軸馬別〈n-2回〉とは異なる** |
| キー集合 | 中央 C(16,2)=120 / P(16,2)=240、地方 C(12,2)=66 / P(12,2)=132 |
| **馬単のキー** | **順序付き**(`0102`=124.4 / `0201`=135.5)。全数走査で全件別値を固定 |
| 未発売時 | 券種によらず同一の `odds_yoso` プレビューにフォールバック |

⚠️ **「馬単キーの1桁目が1着である」ことの決定的な根拠はリポジトリ内に無い**(boss のメタレビュー R1)。
集合一致テストは**原理的に転置を検出できない**(順列集合は転置しても同じ集合)。
**#24-B の着手条件として `result.html` の馬単払戻との突合で確定させる。**


## 次の正式版が 1.9.2 である根拠(Issue #106・#24-B での変更)

**patch**(内部改善。利用者から見てできることは増えず、分析結果の数値も変わらない)。

### 変更内容

`ComboBetType` に `exacta`(馬単)を追加し、キー生成・検証・オッズ取得・払戻保存の全経路を
**betType 別の順序方針**(`COMBO_KEY_ORDER`)に対応させた。

**この変更の危険の本質**: 既存の `buildComboOddsKey` は**常に `.sort()` する**。`COMBO_SIZE` に
`exacta` を足すと `analysis-store.ts` の払戻保存ループ(`COMBO_BET_TYPES` 駆動)が**自動的に
馬単を回し始め**、`1着13・2着8` と `1着8・2着13` が同じ `"0813"` に潰れる。**型エラーは出ない。**

採った設計(#103 の「同一ヘルパに引数で分岐させない」と同じ流儀):

```
buildComboOddsKey(umabans)              既存のまま(ソートする)。順不同専用
buildOrderedComboOddsKey(umabans)       新設(ソートしない)
buildComboOddsKeyFor(betType, umabans)  新設(COMBO_KEY_ORDER で振り分け)
validateComboUmabans / validateOrderedComboUmabans / validateComboUmabansFor
buildComboOddsCellMap / buildComboOddsCellMapFor
```

`betType` を持つ経路(払戻保存ループ・両パーサ)は必ず `...For` を使う。単一馬番の6箇所は無改修。

### patch である根拠(production から到達しない)

- `scrape-race.ts` の `fetchComboBetTypeOdds` 呼び出しは **wide/trio のハードコード2箇所のみ**で、
  **馬単のライブ取得は配線されていない**(配線は #24-D / #24-E)
- `result-import.ts` の `saveResult` 呼び出しは `{ wide, trio }` しか渡さず、`saveResult` のループは
  `combo?.[betType] === undefined` で `continue` するため、**production では exacta 行が1件も
  書き込まれない**
- **`AllocationBetType`(配分側の型)は無変更**。馬単が配分提案・UI・回収率検証に現れる経路は存在しない

### major / minor ではない根拠

**DB スキーマは無変更。** `race_combo_payouts` の PK が `(race_id, bet_type, combo_key)` なので
順序付きキーをそのまま保持でき、列追加もしていない。設定・エクスポート JSON・IPC の後方非互換も無い。

⚠️ この差分で **AC10 とスキーマコメントの「列/テーブル自体を分離する必要がある」を撤回した**
(過剰に強い主張だった)。`grep -rn 'テーブル自体を分離'` で旧文言が残っていないことを確認済み。


## 次の正式版が 1.9.3 である根拠(Issue #107・#24-C での変更)

**patch**(内部改善。利用者から見てできることは増えず、**分析結果の数値も1ビットも変わらない**)。

### 変更内容

`runGreedyAllocation`(`allocation-primitives.ts`)を、**演算列を1つも書き換えずに**定数倍だけ速くした。
馬連・馬単を配分に載せる #24-D / #24-E の前提として、#24 の着手前ゲートが実測した
「馬連候補を足すだけで約5倍遅くなる」への対処である。

入れたものは3つで、いずれも加算の被加数・順序・アキュムレータ値を変えない:

1. **接頭辞和の再利用** —— `indices` に重複が無い不変条件(`OutcomeIndexSet` の JSDoc)により、
   候補 i が現れる位置より前の部分和は候補に依らず不変。`commonWealth` の前方累算の途中経過を
   `prefixSum` に保存して再開する
2. **CSR 化** —— `outcomeIndexSets[].indices` と候補→接触先の逆引きを
   `Int32Array` + オフセットへ。連結するだけなので走査順は不変
3. **バッファの巻き上げ** —— 毎ステップ確保していた配列をループ外で `Float64Array` として
   1回だけ確保し、毎ステップ全要素を書き潰して再利用

### patch である根拠(数値が変わらないことの実地確認)

`docs/versioning.md` の区分表で minor の引き金になるのは「**分析結果の数値が変わる**」ことだが、
本変更は**ビット一致**である:

- `pnpm tsx scripts/bench-mixed-allocation.ts` の greedySteps 感度表(総額・点数・券種構成比)が
  最適化の前後で**1文字も違わず一致**する(オーケストレーターと code-reviewer が独立に確認。
  後者は `git show` による旧ファイル差し替えで再現した)
- `scripts/bench-run-greedy-allocation.ts` の AC-1b 自己検査
  (自前構築の `OutcomeIndexSet[]` による結果と `allocateGeneralBets` の
  `continuousFraction` の `===` 一致)が最適化後も全候補で通る
- 構造的な値(outcome 数・Σ\|indices\|・実際の使用ステップ数・`converged`)が前後で完全に不変
- **#96 で「採用C」を殺した番人**(`bet-allocation.test.ts` の
  「キャップでbetCountが2頭→1頭に減り、notDiversifiedが立つこと」)が、
  **最適化の前と後のどちらでも**採用C相当の変異に対して赤になる(番人が鈍っていない)

### major / minor ではない根拠

DB スキーマ・設定・エクスポート JSON・IPC のいずれも無変更。公開 API の形も変えていない
(`runGreedyAllocation` の返す `fractions` は `number[]` のまま。内部だけ TypedArray)。
利用者から見えるのは「速くなった」ことだけで、**表示される数値・配分額・判定結果はすべて同じ**である。

### 申し送り

馬連・馬単相当を足した規模では、最適化の前後を通じて貪欲ループが `converged=false`
(`greedySteps` を使い切る)のままである。速度だけを変えて収束判定を含む答えを変えていないことの
追加確認でもあるが、**#24-D 以降で `greedySteps`(#36)の議論が必要になる**ことを意味する。


## 次の正式版が 1.9.4 である根拠(Issue #110・#24-C2 での変更)

**patch**(不具合修正・内部改善。利用者から見てできることは増えず、**分析結果の数値も変わらない**)。

### 変更内容

一括分析画面の券種横断の配分計算が、描画の中で同期的に全レース分走り、**その間画面が応答しなくなる**
問題を直した。あわせて、**タブを切り替えて分析タブへ戻るたびに全レースを再計算していた**問題
(`App.tsx` の条件付き描画で画面ごと破棄され、キャッシュも一緒に消えていた)を直した。

- 配分を1レースずつ計算し、レースとレースの間に画面を更新する。未計算のレースには「配分を計算中…」、
  全体の進捗「配分を計算中… n / N レース」を表示する
- キャッシュの持ち主を App 側へ移し、タブを切り替えても設定が変わらなければ再計算しない
- 失敗したレースは一言の注記を出して次へ進み、失敗も記録して同じ条件で再計算を繰り返さない

### patch である根拠

**配分の答え(金額・点数・構成比)は変わらない。** 計算する関数(`buildMixedAllocationDisplay`)も
引数も変更前と同じで、変えたのは「いつ・どこで計算するか」だけである。表示は常に「今の設定」を
キーに照会するので、計算途中で設定が変わっても古い設定の金額は表示しない(変異注入で確認)。
進捗表示は新しい表示要素だが、利用者ができること(分析・配分の確認)は増えていない。

### major / minor ではない根拠

DB スキーマ・設定・エクスポート JSON・IPC のいずれも無変更。

### 残る限界

1レースの計算中は画面が止まる(止まる時間がレース1つ分に縮み、その合間に進捗が更新される)。
完全に止まらなくするには計算を別スレッド(Web Worker)へ移す必要があり、#110 のスコープ外とした。


## 次の正式版が 1.9.5 である根拠(Issue #112・#24-D1 での変更)

**patch**(内部改善。production から到達しない。利用者から見てできることは増えず、分析結果の数値も変わらない)。

### 変更内容

`AllocationBetType` に `quinella`(馬連)を追加し、core の確率・候補ビルダー・配分を対応させた。

- `buildQuinellaCandidates` / `computeQuinellaHitProb`(新設): 上位3着でフィットした θ の順序付き分布から
  `{order[0], order[1]}` を周辺化して馬連の的中確率を求める。**上位3着の集合から求めるとワイドと同じ値になり、
  EV が過大になる**ため。`buildComboCandidates` は馬連を渡すと throw する
- `ALLOCATION_BET_TYPE_REQUIRES_ORDER`(新設): 順序付き outcome 空間を要する券種の唯一の写像。
  `allocateGeneralBets` の門番・的中判定・判定不能時の除外をこれで一般化した
- 順序を要する候補があるとき、`topFinishCount` がその券種の構成頭数以上でなければ throw する

### patch である根拠(production から到達しない)

- app は馬連の候補を作らない(`mixed-candidates.ts` の `ALL_MIXED_CANDIDATE_BET_TYPES` から意図的に除外し、
  テストで固定)。内訳表の表示順配列からも除外した(除外しないと「馬連 ¥0 0点」の行が常に出るため)
- 馬連の候補を含まない入力では結果がビット一致する(`scripts/bench-mixed-allocation.ts` の greedySteps 感度表が
  変更前と1文字も違わないことを確認)

### major / minor ではない根拠

DB スキーマ・設定・エクスポート JSON・IPC のいずれも無変更。


## 次の正式版が 1.9.6 である根拠(Issue #113・#24-D2 での変更)

**patch**(内部改善。production から到達しない。利用者から見てできることは増えず、分析結果の数値も変わらない)。

### 変更内容

`ComboBetType` に `quinella`(馬連)を追加し、オッズのパーサ・取得関数・URL を対応させた
(#106 で馬単を足したのと同じ形)。中央は `api_get_jra_odds` の `type=4`、地方は `odds/index.html?type=b4`。
馬連のキーは昇順に正規化されており(順不同)、値は単一値形式。

### patch である根拠(production から到達しない)

- `scrape-race.ts` の取得処理は従来どおりワイドと3連複だけを呼ぶ(馬連の取得は配線していない。#24-D3)。
  配線しないのは、#24-D3 までの間に使われない馬連オッズの取得が1レース1本ずつ増えるのを避けるため
- 払戻保存ループ(`analysis-store.ts` の `COMBO_BET_TYPES`)は馬連も回すようになったが、
  `result-import.ts` が `{ wide, trio }` しか渡さないので馬連の払戻行は書かれない(テストで固定)

### major / minor ではない根拠

DB スキーマ・設定・エクスポート JSON・IPC のいずれも無変更(`race_combo_payouts` は PK が
`(race_id, bet_type, combo_key)` なので列追加も不要)。


## 次の正式版が 1.9.7 である根拠(Issue #114・#24-F1 での変更)

**patch**(内部改善。利用者から見てできることは増えず、分析結果・検証結果の数値も変わらない)。

### 変更内容

馬連の確定払戻をレース結果から取り込み(`parse-race-result.ts` の `quinellaPayouts` → `result-import.ts`)、
回収率検証(`verify.ts`)で馬連の買い目を判定できるようにした(`ProposedBetReport.quinella`・`overall` への合算)。
ワイド・三連複(#52 + #54)、単勝(#100)と同じ形。

### patch である根拠

- **production から到達する部分**は「結果の取込で馬連の払戻行が `race_combo_payouts` に保存され始める」ことだけで、
  画面には何も出ない
- 配分提案はまだ馬連の買い目を作らない(#24-D3)ので、**検証で馬連が判定される場面は今は無く、
  `overall` を含む検証結果の数値は変わらない**(既存テストの関係式を5項にしても数値は不変)
- 検証画面(`VerifyView.tsx`)には馬連の行を**意図的に出していない**(#24-D3 までは常に0件になるため。#112 の教訓)

### major / minor ではない根拠

DB スキーマ・設定・エクスポート JSON・IPC のいずれも無変更(`race_combo_payouts` の PK は `(race_id, bet_type, combo_key)` で、
馬連の行はそのまま入る)。

## 次の正式版が 1.9.8 である根拠(Issue #115・#24-D3a での変更)

**patch**(内部改善。利用者から見てできることは増えず、分析結果・配分提案の数値も変わらない)。

### 変更内容

設定に「馬連を配分に含める」(`includeQuinellaInAllocation`、**初期値 ON**。CLAUDE.md「新しい券種は配分の対象に
初期値で含める」)を追加し、設定の保存・読込(`settings-store.ts`)・更新(`settings-reducer.ts`)・IPC(`ipc.ts`)・
分析パイプライン(`pipeline-deps.ts`)・配分設定の型(`MixedAllocationSettings`)・配分キャッシュのキー
(`mixed-allocation-cache.ts`・`BatchAnalysisView.tsx`)まで値を運ぶ配線を入れた。

### patch である根拠

- 値は配分計算まで運ばれるが、**券種の選択(`resolveMixedBetTypes`・`isComboBetTypesOff`)はこの値を読まない**。
  配分提案が馬連の買い目を作るのは #24-D3b からで、**配分提案の数値は変わらない**(感度表
  `scripts/bench-mixed-allocation.ts` の出力が #114 時点と同一であることを確認済み)
- 設定画面にトグルはまだ無い(#24-D3b)。利用者がこの設定を見たり変えたりする手段は無い
- 配分キャッシュのキーに項目が1つ増えたが、キャッシュはメモリ上のものでセッションを跨がない

### major / minor ではない根拠

- settings.json は**後方互換**: キーが無い既存の settings.json は初期値(ON)で読まれる
  (`includeWideInAllocation`・`includeTrioInAllocation` と同じ読み方)。読めなくなる設定は無い
- DB スキーマは無変更(配分メタ `analysis_allocation_meta` の列追加は #59 の列凍結との兼ね合いで #24-D3b に回した)
- エクスポート JSON は無変更。IPC の設定ペイロードに項目が増えたが、main と renderer は同じ exe の中で
  同時に更新されるため、版の食い違いは起きない

## 次の正式版が 1.9.9 である根拠(Issue #116・#24-D3b-1 での変更)

**patch**(内部改善。利用者から見てできることは増えず、配分提案の数値も変わらない)。

### 変更内容

「組合せオッズを取得する」(`includeComboOdds`)が ON のとき、ワイド・三連複に続けて**馬連のオッズも取得する**
ようにした(`scrape-race.ts`。1レースあたり +1 リクエスト)。取得した馬連のオッズは分析結果(`AnalysisResult.quinellaCombo`)と
保存用スナップショット(`race_snapshot_json`)に載り、候補ビルダー(`mixed-candidates.ts`)は `betTypes` に馬連が
含まれれば馬連の候補を作れる。

### patch である根拠

- 配分の券種選択(`resolveMixedBetTypes`)と既定の券種一覧(`ALL_MIXED_CANDIDATE_BET_TYPES`)は馬連を含まないため、
  **production の配分計算に馬連の候補は入らない**。配分の感度表(`scripts/bench-mixed-allocation.ts`)は
  #115 時点と同一(code-reviewer が worktree で変更前と比較して確認)
- 利用者から見える変化は、取得のリクエストが1本増えること(`includeComboOdds` ON のときだけ。1.5秒間隔のため
  1レースあたり約1.5秒長くなる)と、馬連の取得に失敗したときに「馬連」の警告が出ることだけ
- 画面・Discord・検証画面には馬連はまだ出ない(#117)

### major / minor ではない根拠

- DB スキーマは無変更(`race_snapshot_json` は JSON 文字列の列で、キーが1つ増えるだけ。`quinellaCombo` を持たない
  過去のスナップショットもそのまま読める)
- 設定・IPC は無変更。エクスポート JSON は無変更(スナップショットの組合せオッズはエクスポート文書に写されない。
  code-reviewer が `buildAnalysisExportDocument` で確認)

## 次の正式版が 1.10.0 である根拠(Issue #117・#24-D3b-2 での変更)

**minor**(利用者から見てできることが増え、配分提案の数値も変わる)。

### 変更内容

馬連を配分提案に含めるようにした。「馬連を馬券配分に使う」(既定 ON。CLAUDE.md「新しい券種は配分の対象に初期値で含める」)が
ON で、組合せオッズの取得が ON のとき、配分の券種選択(`resolveMixedBetTypes`)が馬連を含め、複勝のみへのフォールバック
条件②③も馬連を数える。画面では、一括分析の内訳表・注記、過去分析の提案表示、検証画面の内訳・判定不能の行、
設定画面のトグルに馬連が出る。

### minor である根拠

- **できることが増える**: 馬連の買い目が提案され、設定画面で馬連を配分から外せる
- **数値が変わる**: 組合せオッズの取得が ON のとき、馬連の候補が他の券種と同じ予算枠を分け合うため、既存券種の
  提案額・点数が変わる(#116 のベンチ〈中央16頭・実オッズ〉では 総額85,900円/224点 → 84,300円/260点、馬連22.7%)

### major ではない根拠

- 設定は後方互換(`includeQuinellaInAllocation` は #115 で追加済み。キーが無い settings.json は既定 ON で読まれる)
- DB スキーマは無変更。馬連の買い目は既存の `analysis_bets`(`bet_type` は文字列列)に `"quinella"` として入り、
  #114 の回収率検証がそのまま判定する。配分記録のメタ行に `include_quinella` 列を足すのは #118
- 過去分析の再表示で使う「複勝のみ」の理由文言は、券種名を列挙しない形にした(馬連を評価していない過去の記録にも
  真になるように。メタレビューでの差し戻し)

## 次の正式版が 1.10.1 である根拠(Issue #118・#24-D3b-3 での変更)

**patch**(利用者から見てできることは増えず、分析結果・配分の数値も変わらない)。

### 変更内容

配分記録のメタ行(`analysis_allocation_meta`)に `include_quinella` 列を足し、分析時の「馬連を馬券配分に使う」の
設定を保存するようにした。過去分析の再表示(実効設定の一覧)に「馬連: ON / OFF / 記録なし」を出す。

### patch である根拠

- 配分の計算は無変更。変わるのは「過去分析の実効設定に1行増える」ことだけで、新しい操作は増えない
  (#110〈配分計算の進捗表示〉と同じく、表示の追加は patch として扱う)

### major / minor ではない根拠

- DB は**後方互換**: 既存の DB は起動時に列を後付けする(`PRAGMA table_info` → `ALTER TABLE ADD COLUMN`)。
  列は NULL を許し、列追加より前の記録は「記録なし」と表示する(NOT NULL DEFAULT 0 にすると、v1.10.0 で馬連の
  買い目を保存した記録が「馬連: OFF」と出て、同じ画面の買い目と矛盾するため)。既存の記録は読めなくならない
- 設定・エクスポート JSON・IPC は無変更

## 次の正式版が 1.10.2 である根拠(Issue #119・#24-C3 での変更)

**patch**(内部改善。利用者から見てできることは増えず、配分の数値も変わらない)。

### 変更内容

一括分析画面の配分計算を Web Worker のプールへ移した。計算中も画面が固まらず、複数の Worker でレース単位に並列に計算する
(上限 max(1, min(CPU コア数 − 1, 4)))。Worker 自体が故障したら、従来の画面側の逐次計算に戻る。

### patch である根拠

- **数値は変わらない**: Worker は画面側と同じ関数(`buildMixedAllocationDisplay`)を同じ入力で呼ぶだけ。実 Electron 上で、
  ビルド成果物の Worker チャンクの結果が画面側の直接計算と一致することを確認した(`scripts/verify-worker-pool-electron.mjs`、
  code-reviewer とメインがそれぞれ実行して `correctnessOk: true`)
- 変わるのは速さと応答性だけ(4コアのクラウド環境・12レースで、Worker 1つ 約2.0秒 → 3つ 約0.9秒。メインの実測 2030ms → 899ms)。
  新しい操作は増えない(#110 の進捗表示と同じく patch)

### major / minor ではない根拠

- 設定・DB・エクスポート JSON・IPC は無変更。CSP に `worker-src 'self'` を明示したが、既存の `script-src 'self'` からの
  フォールバックと同じ範囲(明示しない状態でも動くことを実装者が実機で確認)で、許可する範囲は広げていない

## 次の正式版が 1.10.3 である根拠(Issue #120・#24-E1 での変更)

**patch**(内部改善。利用者から見てできることは増えず、配分の数値も変わらない)。

### 変更内容

馬単(exacta)を配分の券種 `AllocationBetType` に加え、core で的中確率 P(1着=a, 2着=b)・候補ビルダー(`buildExactaCandidates`)・
配分計算での扱い(着順どおりの的中判定、順序付きキーでのオッズ引き当て、馬単だけ馬番の昇順要求を外す)を入れた。

### patch である根拠

- app の既定の券種(`ALL_MIXED_CANDIDATE_BET_TYPES`)・内訳の表示順には入れていない(除外をテストで固定)。馬単のオッズもまだ
  取得していない(#122)ため、**production の配分計算に馬単の候補は入らない**。配分の感度表は変更前と同一(code-reviewer が
  変更前のコミットと実行結果を突き合わせて確認)

### major / minor ではない根拠

設定・DB・エクスポート JSON・IPC のいずれも無変更。

## 次の正式版が 1.10.4 である根拠(Issue #121・#24-F2 での変更)

**patch**(内部改善。利用者から見てできることは増えず、検証結果の数値も変わらない)。

### 変更内容

馬単の確定払戻をレース結果から取り込み(`parse-race-result.ts` の `exactaPayouts` → `result-import.ts` → `race_combo_payouts`)、
回収率検証(`verify.ts`)で馬単の買い目を判定できるようにした(`ProposedBetReport.exacta`・`overall` への合算)。払戻パーサは
順序方針に従うようにし、馬単だけ「1着→2着」の順序を保つ(中央 13→8 はキー `1308`)。

### patch である根拠

- production から到達するのは「結果の取込で馬単の払戻行が `race_combo_payouts` に保存され始める」ことだけで、画面には何も出ない
- 配分提案はまだ馬単の買い目を作らない(#123)ので、**検証で馬単が判定される場面は今は無く、`overall` を含む検証結果の数値は
  変わらない**。検証画面(`VerifyView.tsx`)には馬単の行を出していない(#114 の馬連と同じ)
- 既存の券種(ワイド・馬連・三連複)の払戻は、実フィクスチャ8件(中央4・地方4)で組がすべて昇順であることをメインと
  code-reviewer がそれぞれ確認しており、パース結果は変わらない

### major / minor ではない根拠

DB スキーマ・設定・エクスポート JSON・IPC のいずれも無変更(`race_combo_payouts` の PK は `(race_id, bet_type, combo_key)` で、
馬単の行はそのまま入る)。

## 次の正式版が 1.10.5 である根拠(Issue #122・#24-E2 での変更)

**patch**(利用者から見てできることは増えず、配分の数値も変わらない)。

### 変更内容

「組合せオッズを取得する」(`includeComboOdds`)が ON のとき、ワイド・三連複・馬連に続けて**馬単のオッズも取得する**
ようにした(1レースあたり +1 リクエスト)。取得した馬単のオッズ(順序付きキー)は分析結果・保存用スナップショットに載り、
候補ビルダーは `betTypes` に馬単が含まれれば馬単の候補を作れる。設定画面のチェックボックスは「ワイド・馬連・馬単・三連複の
オッズも取得する」に、補足文に馬単の費用と「馬単は現在は取得のみで配分には使いません」を足した。

### patch である根拠

- 配分の券種選択(`resolveMixedBetTypes`)と既定の券種一覧は馬単を含まないため、**production の配分計算に馬単の候補は入らない**。
  配分の感度表は変更前と同一(実装者と code-reviewer がそれぞれ確認)
- 利用者から見える変化は、取得のリクエストが1本増えること(`includeComboOdds` ON のときだけ)、馬単の取得に失敗したときの
  「馬単」の警告、設定画面の文言だけ

### major / minor ではない根拠

設定・DB・エクスポート JSON・IPC のいずれも無変更(`race_snapshot_json` は JSON 文字列の列でキーが1つ増えるだけ)。

## 次の正式版が 1.10.6 である根拠(Issue #124・#24-E3a での変更)

**patch**(内部改善。利用者から見てできることは増えず、配分の数値も変わらない)。

### 変更内容

設定に「馬単を配分に含める」(`includeExactaInAllocation`、**初期値 ON**。CLAUDE.md「新しい券種は配分の対象に初期値で含める」)を
追加し、保存・読込・更新・IPC・分析パイプライン・`MixedAllocationSettings`・配分キャッシュのキー(11項目)まで値を運んだ。
#115(馬連)と同じ形。

### patch である根拠

- 券種の選択(`resolveMixedBetTypes`)・フォールバック規則は**この値を読まない**(値を変えても配分結果が一致することを
  テストで固定。メインも参照箇所を grep で確認)。配分の感度表は変更前と同一(code-reviewer が変更前のコミットと比較)
- 設定画面にトグルはまだ無い(#125)。取得ラベルの「馬単は現在は取得のみで配分には使いません」もそのまま(事実どおり)

### major / minor ではない根拠

- settings.json は**後方互換**: キーが無い既存の settings.json は初期値(ON)で読まれる。真偽値以外の値も初期値に戻す
- DB スキーマは無変更(配分記録のメタ行の列は #126)。エクスポート JSON は無変更。IPC の設定ペイロードに項目が増えたが、
  main と renderer は同じ exe の中で同時に更新される

## 次の正式版が 1.11.0 である根拠(Issue #125・#24-E3b での変更)

**minor**(利用者から見てできることが増え、配分提案の数値も変わる)。

### 変更内容

馬単を配分提案に含めるようにした。「馬単を馬券配分に使う」(既定 ON)が ON で、組合せオッズの取得が ON のとき、配分の券種選択が
馬単を含め、複勝のみへのフォールバック条件②③も馬単を数える。画面では、一括分析の内訳表・注記・買い目一覧、過去分析の提案表示、
検証画面の内訳・判定不能の行、設定画面のトグルに馬単が出る。**馬単の買い目は「13→8」のように着順どおりに表示し**、配分記録にも
着順どおりのキー(`1308`)で保存する(#121 の回収率検証で、逆順の払戻と突き合わさらない)。

### minor である根拠

- **できることが増える**: 馬単の買い目が提案され、設定画面で馬単を配分から外せる
- **数値が変わる**: 組合せオッズの取得が ON のとき、馬単の候補が他の券種と同じ予算枠を分け合うため、既存券種の提案額・点数が
  変わる(#122 のベンチ〈中央16頭・実オッズ〉で 総額84,300円/260点 → 83,800円/273点、馬単1.9%)

### major ではない根拠

- 設定は後方互換(`includeExactaInAllocation` は #124 で追加済み)
- DB スキーマは無変更。馬単の買い目は既存の `analysis_bets`(`bet_type` は文字列列)に `"exacta"`・順序付きキーで入る。
  配分記録のメタ行に `include_exacta` 列を足すのは #126

## 次の正式版が 1.11.1 である根拠(Issue #126・#24-E3c での変更)

**patch**(利用者から見てできることは増えず、分析結果・配分の数値も変わらない)。

### 変更内容

配分記録のメタ行(`analysis_allocation_meta`)に `include_exacta` 列を足し、分析時の「馬単を馬券配分に使う」の
設定を保存するようにした。過去分析の再表示(実効設定の一覧)に「馬単: ON / OFF / 記録なし」を出す(馬連の行の次)。

### patch である根拠

- 配分の計算は無変更。変わるのは「過去分析の実効設定に1行増える」ことだけで、新しい操作は増えない
  (#118〈馬連の同種の変更、1.10.1〉と同じ扱い)

### major / minor ではない根拠

- DB は**後方互換**: 既存の DB は起動時に列を後付けする(`PRAGMA table_info` → `ALTER TABLE ADD COLUMN`、
  `include_quinella` の後付けの後に実行)。列は NULL を許し、列追加より前の記録は「記録なし」と表示する
  (#118 と同じ理由で NOT NULL DEFAULT 0 にしない。v1.11.0 で馬単の買い目を保存した記録が「馬単: OFF」と出て矛盾するため)。
  既存の記録は読めなくならない
- 設定・エクスポート JSON・IPC は無変更

## 関連

- 承認印([PUBLISH-APPROVED])と CI の公開ゲートの詳細は `CLAUDE.md`・
  `.github/workflows/build-windows.yml`(Issue #43)を参照。
- 本規約の不変条件テストは `packages/app/test/version-policy.test.ts`。
