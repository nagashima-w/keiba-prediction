# Issue の着手順

**次に何をやるかは、このファイルを見れば分かる。** セッションが切れて会話の文脈が失われても、ここと GitHub Issue が残る。

最終更新: 2026-08-20(#15 完了時点)

## 読み方

- 上から順に着手する。同じ節の中では番号順ではなく**記載順**が着手順
- 「前提」がある項目は、その前提が完了するまで着手しない
- 完了したら Issue をクローズし、**このファイルからも消す**(open な Issue とこの一覧が食い違ったら、GitHub の方が正)
- 着手しないと決めたものも理由とともに残す。**判断そのものが記録価値を持つ**

## 優先順位の原則(ユーザー指定)

> ワイドと三連複の対応に着手してからはこれらに対応するのを優先して他のissueより先に対応して

**券種拡張シリーズを他の Issue より優先する。** 全体像と技術的な依存関係は **#22** に集約。

例外として **#20**(同時分布モデルの厳密化)は馬単・三連単の技術的前提なのでシリーズの一部として扱う。

---

## 1. 券種拡張シリーズ(最優先)

### 1-1. ワイド・三連複(#22 の第1段)

| 順 | Issue | 内容 | 前提 |
|---|---|---|---|
| 1 | **#29** | 組合せ券種の永続化と回収率検証への反映。DB スキーマ変更が要る | #27(完了済み) |

**この節に残るのは #29 だけになった**(2026-08-20): #15 と #31 が完了したため。両者の経緯は
「参考: 直近で完了したもの」を見ること。**#29 に着手するときは #50(検証側の同型欠陥)を
一緒に片付けるのが自然**(どちらも `verify.ts` を触る)。

### 1-2. 単勝(#22 の第2段)

| 順 | Issue | 内容 | 前提 |
|---|---|---|---|
| 2 | **#23** | 券種拡張: 単勝への対応 | 1-1 完了 |

**最大の論点**: 入力の確率が複勝圏内確率(3着以内)なので、そこから導いた重みで1着確率を計算する妥当性は自明ではない。近似で出すか #20 を待つかを着手前ゲートで判断する。

**#23 の前に片付けるもの**: **#34**(人気の欠損表現。`parse-odds.ts` の JSDoc と実装の食い違い)。優先度は低いが「**人気を扱う次の作業の前**に片付ける」ことが #34 に明記されている。

### 1-3. 馬連・馬単以降(#22 の第3段以降)

| 順 | Issue | 内容 | 前提 |
|---|---|---|---|
| 3 | **#20** | Phase 2: 複勝の同時分布モデルを厳密化する(Plackett-Luce 等) | #23 完了 |
| 4 | **#24** | 券種拡張: 馬連・馬単への対応 | #20(**馬単のみ**。馬連は現モデルで可能と思われる) |
| 5 | **#25** | 券種拡張: 三連単への対応 | #20, #24 |
| 6 | **#26** | 券種拡張: 残りの券種(枠連・枠単ほか) | #25 |

**#24 は分割の可能性がある**: 馬連は現モデルで導けるが、馬単は #20 が前提。馬連だけ先に出す判断を着手前ゲートで検討する。

## 2. 確率の質(#35 系列)

券種拡張とは独立した問題だが、**券種を増やしても確率が無情報なら EV はオッズの順序をなぞるだけ**になるため、放置はできない。ただし優先順位は券種拡張の下。

| 順 | Issue | 内容 | 前提 |
|---|---|---|---|
| 7 | **#39** | 過去レースを分析すると、そのレース自身の結果が戦績に混入する(先読みリーク)。**本番経路の是正** | — |
| 8 | **#41** | #35-1b: サンプルを拡大して確率の質を測定する(中央20+地方10)。**LLM を使う場合はユーザーの事前承認が必要**(課金) | #40(完了済み), #39 |
| 9 | **#42** | #35-2: 較正の要否と方式を決める。**着手しない結論もあり得る** | #41 |
| — | **#35** | 親 Issue。#41 / #42 の結論が出たら本文を更新して閉じる | #42 |
| — | **#36** | 貪欲法の刻み幅 `greedySteps` が券種構成比を左右する。**#35 の較正と交絡するため、較正の見通しが立ってから** | #42 |

**#42 の判定基準**: #41 の Brier スコアの Murphy 分解。**resolution ≈ 0(識別力なし)なら、どんな較正をしても市場超えの回収率は原理的に出ない** → #42 は「較正」ではなく scorer の作り直しとして再定義が必要になる。

## 3. 後回し(着手順は状況次第)

券種拡張シリーズと #35 系列が片付いてから。この節の中の順序は固定しない。

| Issue | 内容 | 備考 |
|---|---|---|
| **#50** | 検証(`verify.ts`)の払戻計算が `placeOddsMin` の値の妥当性を見ていない | #31 の水平展開で発見。**回収率という利用者が意思決定に使う数値を汚染しうる**点で #31 より影響が重いが、到達経路は現状無い。`verify.ts` を触る #29 / #16 / #37 と一緒に片付けるのが自然 |
| **#49** | 一括分析の結果をレース単位で終わり次第そのつど表示する | ユーザー指定(2026-08-20)。現状は `runBatchAnalysis` が全レース終了後にまとめて返し、`BatchProgress` に結果が載っていないため1件も先に描画できない。**レース内のリクエスト進捗は出さない**判断済み |
| **#16** | 機能C-3: 検証画面で「一律100円」と「配分」の回収率を比較する | 券種が増えた後の型で設計すれば作り直しが減る。#29 の後が望ましい |
| **#19** | 正式リリースタグを打つ | **#45 の一致検証は導入済み(タグを打てる状態になった)。** 版数は現在 1.2.1 |
| **#37** | 検証画面のキャリブレーション表示を5%刻みにする | ユーザー指定。変更は定数1つ(`verify.ts:166`)。ただし帯あたりのサンプル数が半減する点の扱いを着手前ゲートで判断 |
| **#30** | 貪欲配分の同値タイブレークを明示規則として定義し直す | **正しさの問題ではなく決定性・可読性の問題**。既存テストの期待値変更を伴う |
| **#46** | README の「v1.0」表記が古い | semver か世代ラベルかの判断が要る |
| **#17** | ドキュメント記載の実測値をテストで守る(Σx*=8.1%) | **単独では着手しない。** `bet-allocation` に触るタスクの受け入れ条件に1行入れて回収する |
| **#18** | リポジトリ内ファイルを読むテストの改行正規化を共通化する | **着手条件を満たした。** #45 で `scripts/test/release-gate.test.ts` が3件目としてリポジトリ内ファイルを読むようになった(`release-workflow-gate.test.ts`・`version-policy.test.ts` と合わせて3件) |

## 4. 着手しない(記録として残す)

| Issue | 理由 |
|---|---|
| **#21** | 将来構想(Cloudflare 移行・スマホからキック・通知)。**現時点では着手しない。** 判断の前提として記録しているもの。Phase 6(discord.js bot)が現行の配布形態では対象外である根拠にもなっている |
| **#22** | 券種拡張のロードマップ。**個別タスクではなく全体像の集約先。** シリーズ完了まで open のまま |

---

## 参考: 直近で完了したもの

次に読む人が「なぜ今この状態なのか」を辿れるように残す。詳細は各 Issue のクローズコメント。

- **#15** — 機能D-3(ワイド・三連複の配分表示)。着手前ゲートの実物調査で、当初の作業項目
  (メモ化・throw の受け皿・買い目ラベル・券種選択 UI 等)が **#28 で既に実装・出荷済み**である
  ことが判明し、大幅に再スコープした。実際に入れたのは買い目一覧の上位20件+折りたたみと、
  文書の矛盾是正。**当初この行に書かれていた「`BatchAnalysisView.tsx:625` が `useMemo` なし」は
  事実誤りだった**(実際のメモ化は `useMemo` ではなく `mixed-allocation-cache.ts` の
  `mixedAllocationCacheRef` で実装済み)。この誤りは `.claude/agents/code-reviewer.md` の観点改訂
  (腐る参照を名前参照へ)の前例2件のうちの1件になっている。UX 判断(上位N件+折りたたみ /
  レース内進捗は出さない / 所要見積りは出さない)はユーザー指定
- **#31** — 複勝配分の候補選定が「値として使えるか」を見ておらず、無関係な1頭の NaN / Infinity が
  レース全体の配分を消していた潜在的欠陥。`isUsableOdds` に判定を1本化した。到達経路は現状無い。
  水平展開で **#50** を発見し、DB 往復の実挙動(NaN だけが自己修復され Infinity は生き残る)を
  回帰テストで固定した
- **#45** — 版数が据え置きなら公開しない CI ゲート(`version-bump-check`)と、`v*` タグ名と version の
  一致検証(`tag-version`)。判定核は `scripts/release-gate.ts` の純関数+依存注入にし、yml は薄い
  呼び出しに留めた(yml のインライン bash だと判定そのものがテストで一度も実行されない)。
  版数を 1.2.1 に上げ、allow 経路を実物(run #212 / exe 更新 / 1.2.0 の孤児掃除 / 検査ステップの
  OK ログ)で確認した。**block 経路は実 CI では未検証**・**`workflow_dispatch` は据え置き検査を
  通らない**・**設定不備は fail-open** が残存ギャップ(#45 のクローズコメントに詳細)
- **#27** — 機能D-2b(ワイド・三連複のオッズ取得)の親 Issue。子(#32 / #33)と後続の #28 がいずれも
  `completed` で閉じており、パーサ層(`packages/core/src/scraper/parse-combo-odds.ts` ほか)と取得配線
  (`fetch-combo-odds.ts`・`scrape-race.ts` のオプトイン `includeComboOdds`)の実在を確認したうえで
  2026-08-19 にクローズした(#27 のクローズコメントに根拠を記載)
- **#40** — 確率の質を測る計測基盤。先読みリーク(#39)を発見した
- **#43** — 印なしコミットが1つ割り込むと未レビューのコードが公開される問題。**公開ゲートを fail-safe に反転**し、承認印 `[PUBLISH-APPROVED]` を導入した
- **#44** — セマンティックバージョニング。版数を 1.2.0 に是正し、`docs/versioning.md` に運用規約を明文化した
